const n = {
  columns: [],
  rows: []
};
const q = (id, diff, kw, p, h1, h2, h3, sql) => ({
  id,
  db: 'music',
  difficulty: diff,
  keywords: kw,
  prompt: p,
  hint1: h1,
  hint2: h2,
  hint3: h3,
  solutionSQL: sql,
  expectedResult: n
});
export const musicQuestions = [q(511, 'easy', ['Select', 'company:Airbnb', 'topic:Aggregate Functions'], 'List all artists with name, genre, and country, ordered by name.', 'SELECT from artists ORDER BY name.', 'ORDER BY name ASC', 'SELECT name, genre, country FROM artists ORDER BY name;', 'SELECT name, genre, country FROM artists ORDER BY name;'), q(512, 'easy', ['Select', 'Where', 'company:Uber', 'topic:Basic SQL'], 'Find all Pop artists. Show name and country.', 'WHERE genre LIKE \'%Pop%\'', 'WHERE genre LIKE \'%Pop%\'', 'SELECT name, country FROM artists WHERE genre LIKE \'%Pop%\' ORDER BY name;', 'SELECT name, country FROM artists WHERE genre LIKE \'%Pop%\' ORDER BY name;'), q(513, 'easy', ['Select', 'company:Apple', 'topic:Database Design'], 'List all albums with title, artist_id, and release_year, ordered by release_year DESC.', 'SELECT from albums ORDER BY release_year DESC.', 'ORDER BY release_year DESC', 'SELECT title, artist_id, release_year FROM albums ORDER BY release_year DESC;', 'SELECT title, artist_id, release_year FROM albums ORDER BY release_year DESC;'), q(514, 'easy', ['Select', 'Group By', 'company:Goldman Sachs', 'topic:Aggregate Functions'], 'Count albums per artist. Show artist_id and album_count.', 'COUNT(*) GROUP BY artist_id on albums.', 'SELECT artist_id, COUNT(*) FROM albums GROUP BY artist_id', 'SELECT artist_id, COUNT(*) AS album_count FROM albums GROUP BY artist_id ORDER BY album_count DESC;', 'SELECT artist_id, COUNT(*) AS album_count FROM albums GROUP BY artist_id ORDER BY album_count DESC;'), q(515, 'easy', ['Select', 'Order By', 'company:TCS', 'topic:Aggregate Functions'], 'List the top 5 longest tracks by duration. Show title and duration_secs.', 'ORDER BY duration_secs DESC LIMIT 5', 'ORDER BY duration_secs DESC LIMIT 5', 'SELECT title, duration_secs FROM tracks ORDER BY duration_secs DESC LIMIT 5;', 'SELECT title, duration_secs FROM tracks ORDER BY duration_secs DESC LIMIT 5;'), q(516, 'easy', ['Select', 'Group By', 'company:Goldman Sachs', 'topic:Aggregate Functions'], 'Count tracks per album. Show album_id and track_count.', 'COUNT(*) GROUP BY album_id on tracks.', 'SELECT album_id, COUNT(*) FROM tracks GROUP BY album_id', 'SELECT album_id, COUNT(*) AS track_count FROM tracks GROUP BY album_id ORDER BY track_count DESC;', 'SELECT album_id, COUNT(*) AS track_count FROM tracks GROUP BY album_id ORDER BY track_count DESC;'), q(517, 'easy', ['Select', 'company:Oracle', 'topic:Database Design'], 'List all users with username and country, ordered by joined_at DESC.', 'SELECT from users ORDER BY joined_at DESC.', 'ORDER BY joined_at DESC', 'SELECT username, country, joined_at FROM users ORDER BY joined_at DESC;', 'SELECT username, country, joined_at FROM users ORDER BY joined_at DESC;'), q(518, 'easy', ['Select', 'company:Stripe', 'topic:Database Design'], 'Count total plays in the database. Return total_plays.', 'COUNT(*) on plays.', 'SELECT COUNT(*) AS total_plays FROM plays', 'SELECT COUNT(*) AS total_plays FROM plays;', 'SELECT COUNT(*) AS total_plays FROM plays;'), q(519, 'easy', ['Select', 'Group By', 'company:Stripe', 'topic:Database Design'], 'Count plays per user. Show user_id and play_count.', 'COUNT(*) GROUP BY user_id on plays.', 'SELECT user_id, COUNT(*) FROM plays GROUP BY user_id', 'SELECT user_id, COUNT(*) AS play_count FROM plays GROUP BY user_id ORDER BY play_count DESC;', 'SELECT user_id, COUNT(*) AS play_count FROM plays GROUP BY user_id ORDER BY play_count DESC;'), q(520, 'easy', ['Select', 'Where', 'company:Google', 'topic:Basic SQL'], 'Find all tracks with BPM above 130 (energetic songs). Show title and bpm.', 'WHERE bpm > 130 ORDER BY bpm DESC', 'WHERE bpm > 130', 'SELECT title, bpm FROM tracks WHERE bpm > 130 ORDER BY bpm DESC;', 'SELECT title, bpm FROM tracks WHERE bpm > 130 ORDER BY bpm DESC;'), q(521, 'easy', ['Select', 'Group By', 'company:Atlassian', 'topic:Keys & Constraints'], 'Count how many playlists each user has. Show user_id and playlist_count.', 'COUNT(*) GROUP BY user_id on playlists.', 'SELECT user_id, COUNT(*) FROM playlists GROUP BY user_id', 'SELECT user_id, COUNT(*) AS playlist_count FROM playlists GROUP BY user_id ORDER BY playlist_count DESC;', 'SELECT user_id, COUNT(*) AS playlist_count FROM playlists GROUP BY user_id ORDER BY playlist_count DESC;'), q(522, 'easy', ['Select', 'Where', 'company:ServiceNow', 'topic:Aggregate Functions'], 'Find all public playlists (is_public = 1). Show name and user_id.', 'WHERE is_public = 1', 'WHERE is_public = 1', 'SELECT name, user_id FROM playlists WHERE is_public = 1;', 'SELECT name, user_id FROM playlists WHERE is_public = 1;'), q(523, 'easy', ['Select', 'Group By', 'company:Microsoft', 'topic:Basic SQL'], 'Count how many tracks are in each playlist. Show playlist_id and track_count.', 'COUNT(*) GROUP BY playlist_id on playlist_tracks.', 'SELECT playlist_id, COUNT(*) FROM playlist_tracks GROUP BY playlist_id', 'SELECT playlist_id, COUNT(*) AS track_count FROM playlist_tracks GROUP BY playlist_id ORDER BY track_count DESC;', 'SELECT playlist_id, COUNT(*) AS track_count FROM playlist_tracks GROUP BY playlist_id ORDER BY track_count DESC;'), q(524, 'easy', ['Select', 'Where', 'company:ServiceNow', 'topic:Database Design'], 'Find all albums released before 2010. Show title and release_year.', 'WHERE release_year < 2010', 'WHERE release_year < 2010 ORDER BY release_year', 'SELECT title, release_year FROM albums WHERE release_year < 2010 ORDER BY release_year;', 'SELECT title, release_year FROM albums WHERE release_year < 2010 ORDER BY release_year;'), q(525, 'easy', ['Select', 'company:Atlassian', 'topic:Basic SQL'], 'Calculate total listening time across all plays (sum of play_duration_secs). Return total_seconds.', 'SUM(play_duration_secs) on plays.', 'SELECT SUM(play_duration_secs) AS total_seconds FROM plays', 'SELECT SUM(play_duration_secs) AS total_seconds FROM plays;', 'SELECT SUM(play_duration_secs) AS total_seconds FROM plays;'), q(526, 'easy', ['Select', 'Group By', 'company:Microsoft', 'topic:Aggregate Functions'], 'Count plays per track. Show track_id and play_count, ordered by play_count DESC.', 'COUNT(*) GROUP BY track_id on plays.', 'SELECT track_id, COUNT(*) FROM plays GROUP BY track_id', 'SELECT track_id, COUNT(*) AS play_count FROM plays GROUP BY track_id ORDER BY play_count DESC;', 'SELECT track_id, COUNT(*) AS play_count FROM plays GROUP BY track_id ORDER BY play_count DESC;'), q(527, 'easy', ['Select', 'Where', 'company:Goldman Sachs', 'topic:Basic SQL'], 'Find tracks with slow BPM (under 70). Show title, album_id, and bpm.', 'WHERE bpm < 70', 'WHERE bpm < 70 ORDER BY bpm', 'SELECT title, album_id, bpm FROM tracks WHERE bpm < 70 ORDER BY bpm;', 'SELECT title, album_id, bpm FROM tracks WHERE bpm < 70 ORDER BY bpm;'), q(528, 'easy', ['Select', 'Group By', 'company:JPMorgan Chase', 'topic:Aggregate Functions'], 'Count plays per date. Show DATE(played_at) and daily_plays.', 'strftime or DATE(played_at) GROUP BY.', 'SELECT DATE(played_at) AS play_date, COUNT(*) FROM plays GROUP BY play_date', 'SELECT DATE(played_at) AS play_date, COUNT(*) AS daily_plays FROM plays GROUP BY play_date ORDER BY play_date;', 'SELECT DATE(played_at) AS play_date, COUNT(*) AS daily_plays FROM plays GROUP BY play_date ORDER BY play_date;'), q(529, 'easy', ['Select', 'Where', 'company:Netflix', 'topic:Database Design'], 'Find all UK artists. Show name and genre.', 'WHERE country = \'UK\'', 'WHERE country = \'UK\'', 'SELECT name, genre FROM artists WHERE country = \'UK\' ORDER BY name;', 'SELECT name, genre FROM artists WHERE country = \'UK\' ORDER BY name;'), q(530, 'easy', ['Select', 'company:Infosys', 'topic:Basic SQL'], 'Show the shortest track. Return title and duration_secs.', 'ORDER BY duration_secs ASC LIMIT 1', 'SELECT title, duration_secs FROM tracks ORDER BY duration_secs ASC LIMIT 1', 'SELECT title, duration_secs FROM tracks ORDER BY duration_secs ASC LIMIT 1;', 'SELECT title, duration_secs FROM tracks ORDER BY duration_secs ASC LIMIT 1;'),
// MEDIUM
q(531, 'medium', ['Join', 'Group By', 'company:Infosys', 'topic:Joins'], 'Show each track with its album title and artist name. Show track title, album title, and artist name.', 'JOIN tracks → albums → artists.', 'JOIN albums ON album_id JOIN artists ON artist_id', 'SELECT t.title AS track_title, al.title AS album_title, ar.name AS artist_name FROM tracks t JOIN albums al ON t.album_id = al.album_id JOIN artists ar ON al.artist_id = ar.artist_id ORDER BY ar.name, al.title;', 'SELECT t.title AS track_title, al.title AS album_title, ar.name AS artist_name FROM tracks t JOIN albums al ON t.album_id = al.album_id JOIN artists ar ON al.artist_id = ar.artist_id ORDER BY ar.name, al.title;'), q(532, 'medium', ['Join', 'Group By', 'company:Atlassian', 'topic:ER Modeling'], 'Calculate total play count per artist. Show artist_name and total_plays.', 'JOIN plays → tracks → albums → artists. COUNT(play_id) GROUP BY artist.', 'COUNT(play_id) GROUP BY artist_id JOIN artists', 'SELECT ar.name AS artist_name, COUNT(p.play_id) AS total_plays FROM artists ar JOIN albums al ON ar.artist_id = al.artist_id JOIN tracks t ON al.album_id = t.album_id JOIN plays p ON t.track_id = p.track_id GROUP BY ar.artist_id ORDER BY total_plays DESC;', 'SELECT ar.name AS artist_name, COUNT(p.play_id) AS total_plays FROM artists ar JOIN albums al ON ar.artist_id = al.artist_id JOIN tracks t ON al.album_id = t.album_id JOIN plays p ON t.track_id = p.track_id GROUP BY ar.artist_id ORDER BY total_plays DESC;'), q(533, 'medium', ['Join', 'Group By', 'company:Airbnb', 'topic:Subqueries'], 'Show each user\'s total listening time in hours. Show username and total_hours.', 'JOIN plays to users, SUM(play_duration_secs) / 3600.', 'SUM(play_duration_secs) / 3600.0 GROUP BY user_id', 'SELECT u.username, ROUND(SUM(p.play_duration_secs) / 3600.0, 2) AS total_hours FROM users u JOIN plays p ON u.user_id = p.user_id GROUP BY u.user_id ORDER BY total_hours DESC;', 'SELECT u.username, ROUND(SUM(p.play_duration_secs) / 3600.0, 2) AS total_hours FROM users u JOIN plays p ON u.user_id = p.user_id GROUP BY u.user_id ORDER BY total_hours DESC;'), q(534, 'medium', ['Join', 'Group By', 'company:Databricks', 'topic:Joins'], 'Find the most-played track. Show title, artist_name, and play_count.', 'JOIN plays → tracks → albums → artists. COUNT GROUP BY track. LIMIT 1.', 'COUNT(play_id) GROUP BY track_id ORDER BY COUNT DESC LIMIT 1', 'SELECT t.title, ar.name AS artist_name, COUNT(p.play_id) AS play_count FROM tracks t JOIN albums al ON t.album_id = al.album_id JOIN artists ar ON al.artist_id = ar.artist_id JOIN plays p ON t.track_id = p.track_id GROUP BY t.track_id ORDER BY play_count DESC LIMIT 1;', 'SELECT t.title, ar.name AS artist_name, COUNT(p.play_id) AS play_count FROM tracks t JOIN albums al ON t.album_id = al.album_id JOIN artists ar ON al.artist_id = ar.artist_id JOIN plays p ON t.track_id = p.track_id GROUP BY t.track_id ORDER BY play_count DESC LIMIT 1;'), q(535, 'medium', ['Left Join', 'Null Handling', 'company:Uber', 'topic:ER Modeling'], 'Find tracks that have never been played. Show title and album_id.', 'LEFT JOIN plays, WHERE play_id IS NULL.', 'LEFT JOIN plays ON track_id WHERE play_id IS NULL', 'SELECT t.title, t.album_id FROM tracks t LEFT JOIN plays p ON t.track_id = p.track_id WHERE p.play_id IS NULL;', 'SELECT t.title, t.album_id FROM tracks t LEFT JOIN plays p ON t.track_id = p.track_id WHERE p.play_id IS NULL;'), q(536, 'medium', ['Join', 'Group By', 'company:Atlassian', 'topic:Joins'], 'Show the most popular genre by total plays. Show genre and play_count.', 'JOIN plays → tracks → albums → artists, GROUP BY genre.', 'COUNT(play_id) GROUP BY genre ORDER BY COUNT DESC LIMIT 1', 'SELECT ar.genre, COUNT(p.play_id) AS play_count FROM artists ar JOIN albums al ON ar.artist_id = al.artist_id JOIN tracks t ON al.album_id = t.album_id JOIN plays p ON t.track_id = p.track_id GROUP BY ar.genre ORDER BY play_count DESC;', 'SELECT ar.genre, COUNT(p.play_id) AS play_count FROM artists ar JOIN albums al ON ar.artist_id = al.artist_id JOIN tracks t ON al.album_id = t.album_id JOIN plays p ON t.track_id = p.track_id GROUP BY ar.genre ORDER BY play_count DESC;'), q(537, 'medium', ['Join', 'Group By', 'company:Apple', 'topic:Execution Order'], 'Show average BPM per genre across all tracks. Show genre and avg_bpm.', 'JOIN tracks → albums → artists. AVG(bpm) GROUP BY genre.', 'AVG(bpm) GROUP BY genre', 'SELECT ar.genre, ROUND(AVG(t.bpm), 1) AS avg_bpm FROM artists ar JOIN albums al ON ar.artist_id = al.artist_id JOIN tracks t ON al.album_id = t.album_id GROUP BY ar.genre ORDER BY avg_bpm DESC;', 'SELECT ar.genre, ROUND(AVG(t.bpm), 1) AS avg_bpm FROM artists ar JOIN albums al ON ar.artist_id = al.artist_id JOIN tracks t ON al.album_id = t.album_id GROUP BY ar.genre ORDER BY avg_bpm DESC;'), q(538, 'medium', ['Join', 'Group By', 'company:Infosys', 'topic:Joins'], 'Find the playlist with the most tracks. Show playlist name and track_count.', 'JOIN playlist_tracks, COUNT GROUP BY playlist_id, LIMIT 1.', 'COUNT(track_id) GROUP BY playlist_id ORDER BY COUNT DESC LIMIT 1', 'SELECT pl.name, COUNT(pt.track_id) AS track_count FROM playlists pl JOIN playlist_tracks pt ON pl.playlist_id = pt.playlist_id GROUP BY pl.playlist_id ORDER BY track_count DESC LIMIT 1;', 'SELECT pl.name, COUNT(pt.track_id) AS track_count FROM playlists pl JOIN playlist_tracks pt ON pl.playlist_id = pt.playlist_id GROUP BY pl.playlist_id ORDER BY track_count DESC LIMIT 1;'), q(539, 'medium', ['Join', 'Group By', 'company:Stripe', 'topic:ER Modeling'], 'Calculate play count per hour of day. Show hour and play_count.', 'strftime(\'%H\', played_at) GROUP BY hour.', 'SELECT strftime(\'%H\', played_at) AS hour, COUNT(*) FROM plays GROUP BY hour', 'SELECT strftime(\'%H\', played_at) AS hour, COUNT(*) AS play_count FROM plays GROUP BY hour ORDER BY hour;', 'SELECT strftime(\'%H\', played_at) AS hour, COUNT(*) AS play_count FROM plays GROUP BY hour ORDER BY hour;'), q(540, 'medium', ['Join', 'Group By', 'company:Atlassian', 'topic:Views'], 'Show total album duration per artist (sum of all track durations). Show artist_name and total_duration_mins.', 'JOIN tracks → albums → artists. SUM(duration_secs) / 60.', 'SUM(duration_secs) / 60.0 GROUP BY artist', 'SELECT ar.name AS artist_name, ROUND(SUM(t.duration_secs) / 60.0, 1) AS total_duration_mins FROM artists ar JOIN albums al ON ar.artist_id = al.artist_id JOIN tracks t ON al.album_id = t.album_id GROUP BY ar.artist_id ORDER BY total_duration_mins DESC;', 'SELECT ar.name AS artist_name, ROUND(SUM(t.duration_secs) / 60.0, 1) AS total_duration_mins FROM artists ar JOIN albums al ON ar.artist_id = al.artist_id JOIN tracks t ON al.album_id = t.album_id GROUP BY ar.artist_id ORDER BY total_duration_mins DESC;'), q(541, 'medium', ['Join', 'Group By', 'company:Meta', 'topic:Joins'], 'Show the completion rate per track play (play_duration / track_duration × 100). Show track_title and avg_completion_pct.', 'AVG(play_duration_secs / duration_secs × 100) GROUP BY track.', 'AVG(p.play_duration_secs * 100.0 / t.duration_secs) GROUP BY track_id', 'SELECT t.title AS track_title, ROUND(AVG(p.play_duration_secs * 100.0 / t.duration_secs), 1) AS avg_completion_pct FROM tracks t JOIN plays p ON t.track_id = p.track_id GROUP BY t.track_id ORDER BY avg_completion_pct DESC;', 'SELECT t.title AS track_title, ROUND(AVG(p.play_duration_secs * 100.0 / t.duration_secs), 1) AS avg_completion_pct FROM tracks t JOIN plays p ON t.track_id = p.track_id GROUP BY t.track_id ORDER BY avg_completion_pct DESC;'), q(542, 'medium', ['Join', 'Group By', 'company:Meta', 'topic:Views'], 'Find users who listen to 3+ different artists. Show username and artist_count.', 'DISTINCT artist_id per user through plays → tracks → albums.', 'COUNT(DISTINCT ar.artist_id) HAVING >= 3', 'SELECT u.username, COUNT(DISTINCT al.artist_id) AS artist_count FROM users u JOIN plays p ON u.user_id = p.user_id JOIN tracks t ON p.track_id = t.track_id JOIN albums al ON t.album_id = al.album_id GROUP BY u.user_id HAVING COUNT(DISTINCT al.artist_id) >= 3 ORDER BY artist_count DESC;', 'SELECT u.username, COUNT(DISTINCT al.artist_id) AS artist_count FROM users u JOIN plays p ON u.user_id = p.user_id JOIN tracks t ON p.track_id = t.track_id JOIN albums al ON t.album_id = al.album_id GROUP BY u.user_id HAVING COUNT(DISTINCT al.artist_id) >= 3 ORDER BY artist_count DESC;'), q(543, 'medium', ['Join', 'Group By', 'company:Meta', 'topic:Execution Order'], 'Show the top artist per user (most plays). Show username and artist_name.', 'ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY play_count DESC) = 1.', 'ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY COUNT(play_id) DESC) = 1', 'SELECT username, artist_name FROM (SELECT u.username, ar.name AS artist_name, COUNT(p.play_id) AS play_count, ROW_NUMBER() OVER (PARTITION BY u.user_id ORDER BY COUNT(p.play_id) DESC) AS rn FROM users u JOIN plays p ON u.user_id = p.user_id JOIN tracks t ON p.track_id = t.track_id JOIN albums al ON t.album_id = al.album_id JOIN artists ar ON al.artist_id = ar.artist_id GROUP BY u.user_id, ar.artist_id) WHERE rn = 1 ORDER BY username;', 'SELECT username, artist_name FROM (SELECT u.username, ar.name AS artist_name, COUNT(p.play_id) AS play_count, ROW_NUMBER() OVER (PARTITION BY u.user_id ORDER BY COUNT(p.play_id) DESC) AS rn FROM users u JOIN plays p ON u.user_id = p.user_id JOIN tracks t ON p.track_id = t.track_id JOIN albums al ON t.album_id = al.album_id JOIN artists ar ON al.artist_id = ar.artist_id GROUP BY u.user_id, ar.artist_id) WHERE rn = 1 ORDER BY username;'), q(544, 'medium', ['Join', 'Group By', 'company:Airbnb', 'topic:ER Modeling'], 'Show plays by country. Show country and total_plays.', 'JOIN users → plays, GROUP BY country.', 'COUNT(play_id) GROUP BY country', 'SELECT u.country, COUNT(p.play_id) AS total_plays FROM users u JOIN plays p ON u.user_id = p.user_id GROUP BY u.country ORDER BY total_plays DESC;', 'SELECT u.country, COUNT(p.play_id) AS total_plays FROM users u JOIN plays p ON u.user_id = p.user_id GROUP BY u.country ORDER BY total_plays DESC;'), q(545, 'medium', ['Join', 'Group By', 'company:Salesforce', 'topic:Execution Order'], 'Find playlists where the combined track duration exceeds 15 minutes. Show playlist_name and total_duration_mins.', 'JOIN playlist_tracks → tracks. SUM(duration_secs) / 60 HAVING > 15.', 'SUM(t.duration_secs) / 60.0 > 15', 'SELECT pl.name AS playlist_name, ROUND(SUM(t.duration_secs) / 60.0, 1) AS total_duration_mins FROM playlists pl JOIN playlist_tracks pt ON pl.playlist_id = pt.playlist_id JOIN tracks t ON pt.track_id = t.track_id GROUP BY pl.playlist_id HAVING SUM(t.duration_secs) / 60.0 > 15 ORDER BY total_duration_mins DESC;', 'SELECT pl.name AS playlist_name, ROUND(SUM(t.duration_secs) / 60.0, 1) AS total_duration_mins FROM playlists pl JOIN playlist_tracks pt ON pl.playlist_id = pt.playlist_id JOIN tracks t ON pt.track_id = t.track_id GROUP BY pl.playlist_id HAVING SUM(t.duration_secs) / 60.0 > 15 ORDER BY total_duration_mins DESC;'), q(546, 'medium', ['Subquery', 'Where', 'company:Stripe', 'topic:ER Modeling'], 'Find tracks that have been played more than the average plays per track. Show title and play_count.', 'HAVING COUNT(play_id) > AVG per track subquery.', 'HAVING COUNT(play_id) > (SELECT AVG(cnt) FROM (SELECT COUNT(*) AS cnt FROM plays GROUP BY track_id))', 'SELECT t.title, COUNT(p.play_id) AS play_count FROM tracks t JOIN plays p ON t.track_id = p.track_id GROUP BY t.track_id HAVING COUNT(p.play_id) > (SELECT AVG(cnt) FROM (SELECT COUNT(*) AS cnt FROM plays GROUP BY track_id)) ORDER BY play_count DESC;', 'SELECT t.title, COUNT(p.play_id) AS play_count FROM tracks t JOIN plays p ON t.track_id = p.track_id GROUP BY t.track_id HAVING COUNT(p.play_id) > (SELECT AVG(cnt) FROM (SELECT COUNT(*) AS cnt FROM plays GROUP BY track_id)) ORDER BY play_count DESC;'), q(547, 'medium', ['Join', 'Group By', 'company:Accenture', 'topic:Joins'], 'Show the ratio of Hip-Hop plays to Pop plays per user. Show user_id and hip_hop_to_pop_ratio.', 'CASE WHEN genre = Hip-Hop COUNT / CASE WHEN genre = Pop COUNT.', 'SUM(CASE WHEN genre=Hip-Hop) / NULLIF(SUM(CASE WHEN genre=Pop), 0)', "SELECT u.user_id, ROUND(CAST(SUM(CASE WHEN ar.genre LIKE '%Hip-Hop%' THEN 1 ELSE 0 END) AS REAL) / NULLIF(SUM(CASE WHEN ar.genre LIKE '%Pop%' AND ar.genre NOT LIKE '%Hip-Hop%' THEN 1 ELSE 0 END), 0), 2) AS hip_hop_to_pop_ratio FROM users u JOIN plays p ON u.user_id = p.user_id JOIN tracks t ON p.track_id = t.track_id JOIN albums al ON t.album_id = al.album_id JOIN artists ar ON al.artist_id = ar.artist_id GROUP BY u.user_id ORDER BY hip_hop_to_pop_ratio DESC;", "SELECT u.user_id, ROUND(CAST(SUM(CASE WHEN ar.genre LIKE '%Hip-Hop%' THEN 1 ELSE 0 END) AS REAL) / NULLIF(SUM(CASE WHEN ar.genre LIKE '%Pop%' AND ar.genre NOT LIKE '%Hip-Hop%' THEN 1 ELSE 0 END), 0), 2) AS hip_hop_to_pop_ratio FROM users u JOIN plays p ON u.user_id = p.user_id JOIN tracks t ON p.track_id = t.track_id JOIN albums al ON t.album_id = al.album_id JOIN artists ar ON al.artist_id = ar.artist_id GROUP BY u.user_id ORDER BY hip_hop_to_pop_ratio DESC;"), q(548, 'medium', ['Join', 'Group By', 'company:Salesforce', 'topic:Subqueries'], 'Show plays per album per user (how many different albums each user played). Show username and albums_played.', 'DISTINCT album_id per user through plays → tracks.', 'COUNT(DISTINCT album_id) GROUP BY user_id', 'SELECT u.username, COUNT(DISTINCT t.album_id) AS albums_played FROM users u JOIN plays p ON u.user_id = p.user_id JOIN tracks t ON p.track_id = t.track_id GROUP BY u.user_id ORDER BY albums_played DESC;', 'SELECT u.username, COUNT(DISTINCT t.album_id) AS albums_played FROM users u JOIN plays p ON u.user_id = p.user_id JOIN tracks t ON p.track_id = t.track_id GROUP BY u.user_id ORDER BY albums_played DESC;'), q(549, 'medium', ['Join', 'Group By', 'company:Stripe', 'topic:Views'], 'Find the most productive year for album releases. Show release_year and album_count.', 'COUNT(*) GROUP BY release_year on albums, ORDER DESC LIMIT 1.', 'SELECT release_year, COUNT(*) FROM albums GROUP BY release_year ORDER BY COUNT DESC LIMIT 1', 'SELECT release_year, COUNT(*) AS album_count FROM albums GROUP BY release_year ORDER BY album_count DESC LIMIT 1;', 'SELECT release_year, COUNT(*) AS album_count FROM albums GROUP BY release_year ORDER BY album_count DESC LIMIT 1;'), q(550, 'medium', ['Join', 'Group By', 'company:Infosys', 'topic:Subqueries'], 'Show the average track count per album by genre. Show genre and avg_tracks_per_album.', 'COUNT tracks per album, then AVG per genre.', 'AVG(track_count) per genre from (COUNT GROUP BY album)', "WITH album_tracks AS (SELECT al.album_id, al.genre, COUNT(t.track_id) AS track_count FROM albums al JOIN tracks t ON al.album_id = t.album_id GROUP BY al.album_id) SELECT genre, ROUND(AVG(track_count), 1) AS avg_tracks_per_album FROM album_tracks GROUP BY genre ORDER BY avg_tracks_per_album DESC;", "WITH album_tracks AS (SELECT al.album_id, al.genre, COUNT(t.track_id) AS track_count FROM albums al JOIN tracks t ON al.album_id = t.album_id GROUP BY al.album_id) SELECT genre, ROUND(AVG(track_count), 1) AS avg_tracks_per_album FROM album_tracks GROUP BY genre ORDER BY avg_tracks_per_album DESC;"),
// HARD (15)
q(551, 'hard', ['Window Function', 'Rank', 'company:Airbnb', 'topic:Locks'], 'Rank tracks by play count using DENSE_RANK. Show title, play_count, and play_rank.', 'DENSE_RANK() OVER (ORDER BY play_count DESC).', 'DENSE_RANK() OVER (ORDER BY play_count DESC) AS play_rank', 'SELECT title, play_count, DENSE_RANK() OVER (ORDER BY play_count DESC) AS play_rank FROM (SELECT t.title, COUNT(p.play_id) AS play_count FROM tracks t LEFT JOIN plays p ON t.track_id = p.track_id GROUP BY t.track_id) ORDER BY play_rank;', 'SELECT title, play_count, DENSE_RANK() OVER (ORDER BY play_count DESC) AS play_rank FROM (SELECT t.title, COUNT(p.play_id) AS play_count FROM tracks t LEFT JOIN plays p ON t.track_id = p.track_id GROUP BY t.track_id) ORDER BY play_rank;'), q(552, 'hard', ['Window Function', 'Lag', 'company:Google', 'topic:Window Functions'], 'Show month-over-month play count change. Show month, monthly_plays, prev_month_plays, and change.', "strftime month GROUP BY, LAG.", "LAG(monthly_plays) OVER (ORDER BY month)", "WITH monthly AS (SELECT strftime('%Y-%m', played_at) AS month, COUNT(*) AS monthly_plays FROM plays GROUP BY month) SELECT month, monthly_plays, LAG(monthly_plays) OVER (ORDER BY month) AS prev_month_plays, monthly_plays - LAG(monthly_plays) OVER (ORDER BY month) AS change FROM monthly ORDER BY month;", "WITH monthly AS (SELECT strftime('%Y-%m', played_at) AS month, COUNT(*) AS monthly_plays FROM plays GROUP BY month) SELECT month, monthly_plays, LAG(monthly_plays) OVER (ORDER BY month) AS prev_month_plays, monthly_plays - LAG(monthly_plays) OVER (ORDER BY month) AS change FROM monthly ORDER BY month;"), q(553, 'hard', ['Window Function', 'Row Number', 'company:Stripe', 'topic:Locks'], 'Find the most played track per user using ROW_NUMBER(). Show username, track_title, play_count where rn=1.', 'ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY play_count DESC) WHERE rn=1.', 'ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY count DESC) AS rn WHERE rn=1', 'SELECT username, track_title, play_count FROM (SELECT u.username, t.title AS track_title, COUNT(p.play_id) AS play_count, ROW_NUMBER() OVER (PARTITION BY u.user_id ORDER BY COUNT(p.play_id) DESC) AS rn FROM users u JOIN plays p ON u.user_id = p.user_id JOIN tracks t ON p.track_id = t.track_id GROUP BY u.user_id, t.track_id) WHERE rn = 1 ORDER BY username;', 'SELECT username, track_title, play_count FROM (SELECT u.username, t.title AS track_title, COUNT(p.play_id) AS play_count, ROW_NUMBER() OVER (PARTITION BY u.user_id ORDER BY COUNT(p.play_id) DESC) AS rn FROM users u JOIN plays p ON u.user_id = p.user_id JOIN tracks t ON p.track_id = t.track_id GROUP BY u.user_id, t.track_id) WHERE rn = 1 ORDER BY username;'), q(554, 'hard', ['Window Function', 'CTE', 'company:Uber', 'topic:Indexing'], 'Calculate running play count per user (cumulative plays over time). Show user_id, played_at, and cumulative_plays.', 'ROW_NUMBER or SUM(1) OVER (PARTITION BY user_id ORDER BY played_at).', 'ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY played_at) AS cumulative_plays', 'SELECT user_id, played_at, ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY played_at) AS cumulative_plays FROM plays ORDER BY user_id, played_at;', 'SELECT user_id, played_at, ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY played_at) AS cumulative_plays FROM plays ORDER BY user_id, played_at;'), q(555, 'hard', ['Window Function', 'Rank', 'company:Infosys', 'topic:Indexing'], 'Calculate each artist\'s share of total plays as a percentage. Show artist_name, play_count, and pct_of_total.', 'play_count / SUM(play_count) OVER () * 100.', 'play_count / SUM(play_count) OVER () * 100 AS pct_of_total', 'SELECT artist_name, play_count, ROUND(100.0 * play_count / SUM(play_count) OVER (), 2) AS pct_of_total FROM (SELECT ar.name AS artist_name, COUNT(p.play_id) AS play_count FROM artists ar JOIN albums al ON ar.artist_id = al.artist_id JOIN tracks t ON al.album_id = t.album_id JOIN plays p ON t.track_id = p.track_id GROUP BY ar.artist_id) ORDER BY play_count DESC;', 'SELECT artist_name, play_count, ROUND(100.0 * play_count / SUM(play_count) OVER (), 2) AS pct_of_total FROM (SELECT ar.name AS artist_name, COUNT(p.play_id) AS play_count FROM artists ar JOIN albums al ON ar.artist_id = al.artist_id JOIN tracks t ON al.album_id = t.album_id JOIN plays p ON t.track_id = p.track_id GROUP BY ar.artist_id) ORDER BY play_count DESC;'), q(556, 'hard', ['Window Function', 'Lead', 'company:Oracle', 'topic:Query Optimization'], 'Show listening session gaps per user: time between consecutive plays. Show user_id, played_at, next_played_at, and gap_minutes.', 'LEAD(played_at) OVER (PARTITION BY user_id ORDER BY played_at). julianday × 1440.', 'LEAD(played_at) OVER (PARTITION BY user_id ORDER BY played_at) AS next_played_at', 'SELECT user_id, played_at, LEAD(played_at) OVER (PARTITION BY user_id ORDER BY played_at) AS next_played_at, ROUND((julianday(LEAD(played_at) OVER (PARTITION BY user_id ORDER BY played_at)) - julianday(played_at)) * 1440, 1) AS gap_minutes FROM plays ORDER BY user_id, played_at;', 'SELECT user_id, played_at, LEAD(played_at) OVER (PARTITION BY user_id ORDER BY played_at) AS next_played_at, ROUND((julianday(LEAD(played_at) OVER (PARTITION BY user_id ORDER BY played_at)) - julianday(played_at)) * 1440, 1) AS gap_minutes FROM plays ORDER BY user_id, played_at;'), q(557, 'hard', ['CTE', 'Window Function', 'company:Amazon', 'topic:Transactions'], 'Find the fastest-growing track (most plays in 2024-02 vs 2024-01). Show title, jan_plays, feb_plays, and growth.', 'Conditional COUNT per month, growth = feb - jan.', 'SUM(CASE WHEN month=01) AS jan_plays, SUM(CASE WHEN month=02) AS feb_plays', "WITH monthly AS (SELECT p.track_id, SUM(CASE WHEN strftime('%Y-%m', p.played_at) = '2024-01' THEN 1 ELSE 0 END) AS jan_plays, SUM(CASE WHEN strftime('%Y-%m', p.played_at) = '2024-02' THEN 1 ELSE 0 END) AS feb_plays FROM plays p GROUP BY p.track_id) SELECT t.title, m.jan_plays, m.feb_plays, m.feb_plays - m.jan_plays AS growth FROM monthly m JOIN tracks t ON m.track_id = t.track_id ORDER BY growth DESC;", "WITH monthly AS (SELECT p.track_id, SUM(CASE WHEN strftime('%Y-%m', p.played_at) = '2024-01' THEN 1 ELSE 0 END) AS jan_plays, SUM(CASE WHEN strftime('%Y-%m', p.played_at) = '2024-02' THEN 1 ELSE 0 END) AS feb_plays FROM plays p GROUP BY p.track_id) SELECT t.title, m.jan_plays, m.feb_plays, m.feb_plays - m.jan_plays AS growth FROM monthly m JOIN tracks t ON m.track_id = t.track_id ORDER BY growth DESC;"), q(558, 'hard', ['Window Function', 'CTE', 'company:JPMorgan Chase', 'topic:Locks'], 'Calculate listening diversity score per user: ratio of unique tracks to total plays. Show username, total_plays, unique_tracks, diversity_score.', 'COUNT(DISTINCT track_id) / COUNT(play_id) × 100.', 'COUNT(DISTINCT track_id) / COUNT(play_id) * 100 AS diversity_score', 'SELECT u.username, COUNT(p.play_id) AS total_plays, COUNT(DISTINCT p.track_id) AS unique_tracks, ROUND(100.0 * COUNT(DISTINCT p.track_id) / COUNT(p.play_id), 1) AS diversity_score FROM users u JOIN plays p ON u.user_id = p.user_id GROUP BY u.user_id ORDER BY diversity_score DESC;', 'SELECT u.username, COUNT(p.play_id) AS total_plays, COUNT(DISTINCT p.track_id) AS unique_tracks, ROUND(100.0 * COUNT(DISTINCT p.track_id) / COUNT(p.play_id), 1) AS diversity_score FROM users u JOIN plays p ON u.user_id = p.user_id GROUP BY u.user_id ORDER BY diversity_score DESC;'), q(559, 'hard', ['Window Function', 'Rank', 'company:Apple', 'topic:CTEs'], 'Use NTILE(3) to segment tracks into tiers by play count. Show title, play_count, and tier (1=most played, 3=least played).', 'NTILE(3) OVER (ORDER BY play_count DESC).', 'NTILE(3) OVER (ORDER BY play_count DESC) AS tier', 'SELECT title, play_count, NTILE(3) OVER (ORDER BY play_count DESC) AS tier FROM (SELECT t.title, COUNT(p.play_id) AS play_count FROM tracks t LEFT JOIN plays p ON t.track_id = p.track_id GROUP BY t.track_id) ORDER BY tier, play_count DESC;', 'SELECT title, play_count, NTILE(3) OVER (ORDER BY play_count DESC) AS tier FROM (SELECT t.title, COUNT(p.play_id) AS play_count FROM tracks t LEFT JOIN plays p ON t.track_id = p.track_id GROUP BY t.track_id) ORDER BY tier, play_count DESC;'), q(560, 'hard', ['CTE', 'Window Function', 'company:Google', 'topic:Transactions'], 'Identify users who exclusively listen to one genre (all their plays are from one genre). Show username and genre.', 'COUNT(DISTINCT genre) = 1 per user.', 'HAVING COUNT(DISTINCT ar.genre) = 1', 'SELECT u.username, MAX(ar.genre) AS genre FROM users u JOIN plays p ON u.user_id = p.user_id JOIN tracks t ON p.track_id = t.track_id JOIN albums al ON t.album_id = al.album_id JOIN artists ar ON al.artist_id = ar.artist_id GROUP BY u.user_id HAVING COUNT(DISTINCT ar.genre) = 1 ORDER BY username;', 'SELECT u.username, MAX(ar.genre) AS genre FROM users u JOIN plays p ON u.user_id = p.user_id JOIN tracks t ON p.track_id = t.track_id JOIN albums al ON t.album_id = al.album_id JOIN artists ar ON al.artist_id = ar.artist_id GROUP BY u.user_id HAVING COUNT(DISTINCT ar.genre) = 1 ORDER BY username;'), q(561, 'hard', ['Window Function', 'CTE', 'company:Amazon', 'topic:Query Optimization'], 'Calculate a 3-day rolling average of plays. Show play_date, daily_plays, and rolling_avg_3day.', 'AVG OVER (3-day window).', 'AVG(daily_plays) OVER (ORDER BY play_date ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)', "WITH daily AS (SELECT DATE(played_at) AS play_date, COUNT(*) AS daily_plays FROM plays GROUP BY DATE(played_at)) SELECT play_date, daily_plays, ROUND(AVG(daily_plays) OVER (ORDER BY play_date ROWS BETWEEN 2 PRECEDING AND CURRENT ROW), 1) AS rolling_avg_3day FROM daily ORDER BY play_date;", "WITH daily AS (SELECT DATE(played_at) AS play_date, COUNT(*) AS daily_plays FROM plays GROUP BY DATE(played_at)) SELECT play_date, daily_plays, ROUND(AVG(daily_plays) OVER (ORDER BY play_date ROWS BETWEEN 2 PRECEDING AND CURRENT ROW), 1) AS rolling_avg_3day FROM daily ORDER BY play_date;"), q(562, 'hard', ['Window Function', 'Rank', 'company:Amazon', 'topic:Indexing'], 'For each user, show their first ever played track and their most recent (using FIRST_VALUE and LAST_VALUE / ROW_NUMBER). Show username, first_track, and latest_track.', 'ROW_NUMBER() ASC for first, ROW_NUMBER() DESC for latest. Join to track title.', 'ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY played_at ASC) = 1 and DESC = 1', "WITH first_play AS (SELECT user_id, track_id FROM (SELECT user_id, track_id, ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY played_at ASC) AS rn FROM plays) WHERE rn = 1), last_play AS (SELECT user_id, track_id FROM (SELECT user_id, track_id, ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY played_at DESC) AS rn FROM plays) WHERE rn = 1) SELECT u.username, tf.title AS first_track, tl.title AS latest_track FROM users u JOIN first_play fp ON u.user_id = fp.user_id JOIN last_play lp ON u.user_id = lp.user_id JOIN tracks tf ON fp.track_id = tf.track_id JOIN tracks tl ON lp.track_id = tl.track_id ORDER BY u.username;", "WITH first_play AS (SELECT user_id, track_id FROM (SELECT user_id, track_id, ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY played_at ASC) AS rn FROM plays) WHERE rn = 1), last_play AS (SELECT user_id, track_id FROM (SELECT user_id, track_id, ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY played_at DESC) AS rn FROM plays) WHERE rn = 1) SELECT u.username, tf.title AS first_track, tl.title AS latest_track FROM users u JOIN first_play fp ON u.user_id = fp.user_id JOIN last_play lp ON u.user_id = lp.user_id JOIN tracks tf ON fp.track_id = tf.track_id JOIN tracks tl ON lp.track_id = tl.track_id ORDER BY u.username;"), q(563, 'hard', ['CTE', 'Window Function', 'Rank', 'company:Goldman Sachs', 'topic:Indexing'], 'Build a music recommendation score: for each track, score = play_count × 10 + avg_completion_pct × 5. Show title, play_count, avg_completion_pct, recommendation_score, and rank.', 'Multiple CTEs. compute score. RANK() OVER DESC.', 'RANK() OVER (ORDER BY recommendation_score DESC)', "WITH plays_data AS (SELECT p.track_id, COUNT(*) AS play_count, ROUND(AVG(p.play_duration_secs * 100.0 / t.duration_secs), 1) AS avg_completion_pct FROM plays p JOIN tracks t ON p.track_id = t.track_id GROUP BY p.track_id) SELECT t.title, pd.play_count, pd.avg_completion_pct, ROUND(pd.play_count * 10 + pd.avg_completion_pct * 5, 1) AS recommendation_score, RANK() OVER (ORDER BY pd.play_count * 10 + pd.avg_completion_pct * 5 DESC) AS rec_rank FROM tracks t JOIN plays_data pd ON t.track_id = pd.track_id ORDER BY rec_rank;", "WITH plays_data AS (SELECT p.track_id, COUNT(*) AS play_count, ROUND(AVG(p.play_duration_secs * 100.0 / t.duration_secs), 1) AS avg_completion_pct FROM plays p JOIN tracks t ON p.track_id = t.track_id GROUP BY p.track_id) SELECT t.title, pd.play_count, pd.avg_completion_pct, ROUND(pd.play_count * 10 + pd.avg_completion_pct * 5, 1) AS recommendation_score, RANK() OVER (ORDER BY pd.play_count * 10 + pd.avg_completion_pct * 5 DESC) AS rec_rank FROM tracks t JOIN plays_data pd ON t.track_id = pd.track_id ORDER BY rec_rank;"), q(564, 'hard', ['Window Function', 'CTE', 'company:Infosys', 'topic:Window Functions'], 'Compute artist market share growth: compare each artist\'s play share in Jan vs Feb 2024. Show artist_name, jan_pct, feb_pct, and growth_pct.', 'Play count per artist per month. Divide by monthly total. Compare.', 'SUM(jan plays) / total_jan * 100 AS jan_pct, SUM(feb plays) / total_feb * 100 AS feb_pct', "WITH monthly AS (SELECT al.artist_id, SUM(CASE WHEN strftime('%Y-%m', p.played_at) = '2024-01' THEN 1 ELSE 0 END) AS jan_plays, SUM(CASE WHEN strftime('%Y-%m', p.played_at) = '2024-02' THEN 1 ELSE 0 END) AS feb_plays FROM plays p JOIN tracks t ON p.track_id = t.track_id JOIN albums al ON t.album_id = al.album_id GROUP BY al.artist_id), totals AS (SELECT SUM(CASE WHEN strftime('%Y-%m', played_at) = '2024-01' THEN 1 ELSE 0 END) AS total_jan, SUM(CASE WHEN strftime('%Y-%m', played_at) = '2024-02' THEN 1 ELSE 0 END) AS total_feb FROM plays) SELECT ar.name AS artist_name, ROUND(100.0 * m.jan_plays / NULLIF(t.total_jan, 0), 1) AS jan_pct, ROUND(100.0 * m.feb_plays / NULLIF(t.total_feb, 0), 1) AS feb_pct, ROUND(100.0 * m.feb_plays / NULLIF(t.total_feb, 0), 1) - ROUND(100.0 * m.jan_plays / NULLIF(t.total_jan, 0), 1) AS growth_pct FROM monthly m JOIN artists ar ON m.artist_id = ar.artist_id CROSS JOIN totals t ORDER BY growth_pct DESC;", "WITH monthly AS (SELECT al.artist_id, SUM(CASE WHEN strftime('%Y-%m', p.played_at) = '2024-01' THEN 1 ELSE 0 END) AS jan_plays, SUM(CASE WHEN strftime('%Y-%m', p.played_at) = '2024-02' THEN 1 ELSE 0 END) AS feb_plays FROM plays p JOIN tracks t ON p.track_id = t.track_id JOIN albums al ON t.album_id = al.album_id GROUP BY al.artist_id), totals AS (SELECT SUM(CASE WHEN strftime('%Y-%m', played_at) = '2024-01' THEN 1 ELSE 0 END) AS total_jan, SUM(CASE WHEN strftime('%Y-%m', played_at) = '2024-02' THEN 1 ELSE 0 END) AS total_feb FROM plays) SELECT ar.name AS artist_name, ROUND(100.0 * m.jan_plays / NULLIF(t.total_jan, 0), 1) AS jan_pct, ROUND(100.0 * m.feb_plays / NULLIF(t.total_feb, 0), 1) AS feb_pct, ROUND(100.0 * m.feb_plays / NULLIF(t.total_feb, 0), 1) - ROUND(100.0 * m.jan_plays / NULLIF(t.total_jan, 0), 1) AS growth_pct FROM monthly m JOIN artists ar ON m.artist_id = ar.artist_id CROSS JOIN totals t ORDER BY growth_pct DESC;"), q(565, 'hard', ['CTE', 'Window Function', 'Rank', 'company:Accenture', 'topic:Transactions'], 'Create a complete user engagement report: show username, total_plays, unique_artists, total_hours, diversity_score, and engagement_rank using RANK().', 'Multiple CTEs: plays, artists, hours, diversity. Compute RANK().', 'RANK() OVER (ORDER BY engagement_score DESC)', "WITH stats AS (SELECT p.user_id, COUNT(p.play_id) AS total_plays, COUNT(DISTINCT al.artist_id) AS unique_artists, ROUND(SUM(p.play_duration_secs) / 3600.0, 2) AS total_hours, ROUND(100.0 * COUNT(DISTINCT p.track_id) / COUNT(p.play_id), 1) AS diversity_score FROM plays p JOIN tracks t ON p.track_id = t.track_id JOIN albums al ON t.album_id = al.album_id GROUP BY p.user_id) SELECT u.username, s.total_plays, s.unique_artists, s.total_hours, s.diversity_score, RANK() OVER (ORDER BY s.total_plays + s.unique_artists * 5 + s.diversity_score DESC) AS engagement_rank FROM users u JOIN stats s ON u.user_id = s.user_id ORDER BY engagement_rank;", "WITH stats AS (SELECT p.user_id, COUNT(p.play_id) AS total_plays, COUNT(DISTINCT al.artist_id) AS unique_artists, ROUND(SUM(p.play_duration_secs) / 3600.0, 2) AS total_hours, ROUND(100.0 * COUNT(DISTINCT p.track_id) / COUNT(p.play_id), 1) AS diversity_score FROM plays p JOIN tracks t ON p.track_id = t.track_id JOIN albums al ON t.album_id = al.album_id GROUP BY p.user_id) SELECT u.username, s.total_plays, s.unique_artists, s.total_hours, s.diversity_score, RANK() OVER (ORDER BY s.total_plays + s.unique_artists * 5 + s.diversity_score DESC) AS engagement_rank FROM users u JOIN stats s ON u.user_id = s.user_id ORDER BY engagement_rank;")];
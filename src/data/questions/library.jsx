const n = {
  columns: [],
  rows: []
};
const q = (id, diff, kw, p, h1, h2, h3, sql) => ({
  id,
  db: 'library',
  difficulty: diff,
  keywords: kw,
  prompt: p,
  hint1: h1,
  hint2: h2,
  hint3: h3,
  solutionSQL: sql,
  expectedResult: n
});
export const libraryQuestions = [
// EASY (20)
q(401, 'easy', ['Select', 'company:Google', 'topic:Aggregate Functions'], 'List all books with title, published_year, and genre, ordered by published_year.', 'SELECT from books.', 'ORDER BY published_year ASC', 'SELECT title, published_year, genre FROM books ORDER BY published_year;', 'SELECT title, published_year, genre FROM books ORDER BY published_year;'), q(402, 'easy', ['Select', 'Where', 'company:Apple', 'topic:Keys & Constraints'], 'Find all books in the Fantasy genre. Show title and author_id.', 'WHERE genre = \'Fantasy\'', 'WHERE genre = \'Fantasy\'', 'SELECT title, author_id FROM books WHERE genre = \'Fantasy\' ORDER BY title;', 'SELECT title, author_id FROM books WHERE genre = \'Fantasy\' ORDER BY title;'), q(403, 'easy', ['Select', 'company:ServiceNow', 'topic:Keys & Constraints'], 'List all library members with first_name, last_name, and joined_date, ordered by joined_date DESC.', 'SELECT from members ORDER BY joined_date DESC.', 'ORDER BY joined_date DESC', 'SELECT first_name, last_name, joined_date FROM members ORDER BY joined_date DESC;', 'SELECT first_name, last_name, joined_date FROM members ORDER BY joined_date DESC;'), q(404, 'easy', ['Select', 'Where', 'Null Handling', 'company:Microsoft', 'topic:Database Design'], 'Find all active loans (return_date IS NULL). Show loan_id, book_id, member_id, loan_date.', 'WHERE return_date IS NULL', 'WHERE return_date IS NULL', 'SELECT loan_id, book_id, member_id, loan_date FROM loans WHERE return_date IS NULL;', 'SELECT loan_id, book_id, member_id, loan_date FROM loans WHERE return_date IS NULL;'), q(405, 'easy', ['Select', 'Group By', 'company:NVIDIA', 'topic:Database Design'], 'Count how many books each author has written. Show author_id and book_count.', 'COUNT(*) GROUP BY author_id.', 'SELECT author_id, COUNT(*) FROM books GROUP BY author_id', 'SELECT author_id, COUNT(*) AS book_count FROM books GROUP BY author_id ORDER BY book_count DESC;', 'SELECT author_id, COUNT(*) AS book_count FROM books GROUP BY author_id ORDER BY book_count DESC;'), q(406, 'easy', ['Select', 'Where', 'company:NVIDIA', 'topic:Aggregate Functions'], 'Show all overdue loans (return_date is after due_date). Show loan_id, book_id, due_date, return_date.', 'WHERE return_date > due_date', 'WHERE return_date > due_date', 'SELECT loan_id, book_id, due_date, return_date FROM loans WHERE return_date > due_date;', 'SELECT loan_id, book_id, due_date, return_date FROM loans WHERE return_date > due_date;'), q(407, 'easy', ['Select', 'company:Atlassian', 'topic:Aggregate Functions'], 'List all library branches with name and location.', 'SELECT from branches.', 'SELECT name, location FROM branches', 'SELECT name, location FROM branches;', 'SELECT name, location FROM branches;'), q(408, 'easy', ['Select', 'Group By', 'company:Microsoft', 'topic:Keys & Constraints'], 'Count members per branch. Show branch_id and member_count.', 'COUNT(*) GROUP BY branch_id on members.', 'SELECT branch_id, COUNT(*) FROM members GROUP BY branch_id', 'SELECT branch_id, COUNT(*) AS member_count FROM members GROUP BY branch_id ORDER BY member_count DESC;', 'SELECT branch_id, COUNT(*) AS member_count FROM members GROUP BY branch_id ORDER BY member_count DESC;'), q(409, 'easy', ['Select', 'Order By', 'company:TCS', 'topic:Keys & Constraints'], 'List the 5 thickest books (most pages). Show title and pages.', 'ORDER BY pages DESC LIMIT 5', 'ORDER BY pages DESC LIMIT 5', 'SELECT title, pages FROM books ORDER BY pages DESC LIMIT 5;', 'SELECT title, pages FROM books ORDER BY pages DESC LIMIT 5;'), q(410, 'easy', ['Select', 'company:Netflix', 'topic:Basic SQL'], 'Count total number of loans in the database. Return total_loans.', 'COUNT(*) on loans.', 'SELECT COUNT(*) AS total_loans FROM loans', 'SELECT COUNT(*) AS total_loans FROM loans;', 'SELECT COUNT(*) AS total_loans FROM loans;'), q(411, 'easy', ['Select', 'Where', 'company:Google', 'topic:Database Design'], 'Find all British authors. Show first_name, last_name, and birth_year.', 'WHERE nationality = \'British\'', 'WHERE nationality = \'British\'', 'SELECT first_name, last_name, birth_year FROM authors WHERE nationality = \'British\' ORDER BY last_name;', 'SELECT first_name, last_name, birth_year FROM authors WHERE nationality = \'British\' ORDER BY last_name;'), q(412, 'easy', ['Select', 'Group By', 'company:Meta', 'topic:Aggregate Functions'], 'Count books by genre. Show genre and count, ordered by count DESC.', 'GROUP BY genre COUNT(*).', 'SELECT genre, COUNT(*) FROM books GROUP BY genre', 'SELECT genre, COUNT(*) AS book_count FROM books GROUP BY genre ORDER BY book_count DESC;', 'SELECT genre, COUNT(*) AS book_count FROM books GROUP BY genre ORDER BY book_count DESC;'), q(413, 'easy', ['Select', 'Where', 'company:Infosys', 'topic:Database Design'], 'Find all unpaid fines (paid_date IS NULL). Show fine_id, loan_id, amount.', 'WHERE paid_date IS NULL', 'WHERE paid_date IS NULL', 'SELECT fine_id, loan_id, amount FROM fines WHERE paid_date IS NULL;', 'SELECT fine_id, loan_id, amount FROM fines WHERE paid_date IS NULL;'), q(414, 'easy', ['Select', 'company:Amazon', 'topic:Aggregate Functions'], 'Calculate total fines collected. Return total_fines.', 'SUM(amount) on fines.', 'SELECT SUM(amount) AS total_fines FROM fines', 'SELECT SUM(amount) AS total_fines FROM fines;', 'SELECT SUM(amount) AS total_fines FROM fines;'), q(415, 'easy', ['Select', 'Where', 'company:Databricks', 'topic:Aggregate Functions'], 'Find all loans for member_id = 1. Show loan_id, book_id, loan_date, return_date.', 'WHERE member_id = 1', 'WHERE member_id = 1', 'SELECT loan_id, book_id, loan_date, return_date FROM loans WHERE member_id = 1;', 'SELECT loan_id, book_id, loan_date, return_date FROM loans WHERE member_id = 1;'), q(416, 'easy', ['Select', 'Where', 'company:ServiceNow', 'topic:Database Design'], 'Find books published before 1900. Show title and published_year.', 'WHERE published_year < 1900', 'WHERE published_year < 1900 ORDER BY published_year', 'SELECT title, published_year FROM books WHERE published_year < 1900 ORDER BY published_year;', 'SELECT title, published_year FROM books WHERE published_year < 1900 ORDER BY published_year;'), q(417, 'easy', ['Select', 'company:Airbnb', 'topic:Database Design'], 'List all distinct genres in the library. Show genre, ordered alphabetically.', 'DISTINCT genre from books.', 'SELECT DISTINCT genre FROM books ORDER BY genre', 'SELECT DISTINCT genre FROM books ORDER BY genre;', 'SELECT DISTINCT genre FROM books ORDER BY genre;'), q(418, 'easy', ['Select', 'Group By', 'company:Amazon', 'topic:Aggregate Functions'], 'Count total loans per member. Show member_id and loan_count.', 'COUNT(*) GROUP BY member_id on loans.', 'SELECT member_id, COUNT(*) FROM loans GROUP BY member_id', 'SELECT member_id, COUNT(*) AS loan_count FROM loans GROUP BY member_id ORDER BY loan_count DESC;', 'SELECT member_id, COUNT(*) AS loan_count FROM loans GROUP BY member_id ORDER BY loan_count DESC;'), q(419, 'easy', ['Select', 'Where', 'company:Stripe', 'topic:Keys & Constraints'], 'Find all books with more than 500 pages. Show title and pages.', 'WHERE pages > 500 ORDER BY pages DESC', 'WHERE pages > 500', 'SELECT title, pages FROM books WHERE pages > 500 ORDER BY pages DESC;', 'SELECT title, pages FROM books WHERE pages > 500 ORDER BY pages DESC;'), q(420, 'easy', ['Select', 'Group By', 'company:Amazon', 'topic:Basic SQL'], 'Calculate average book length (pages) per genre. Show genre and avg_pages.', 'AVG(pages) GROUP BY genre.', 'SELECT genre, AVG(pages) FROM books GROUP BY genre', 'SELECT genre, ROUND(AVG(pages), 0) AS avg_pages FROM books GROUP BY genre ORDER BY avg_pages DESC;', 'SELECT genre, ROUND(AVG(pages), 0) AS avg_pages FROM books GROUP BY genre ORDER BY avg_pages DESC;'),
// MEDIUM (25)
q(421, 'medium', ['Join', 'Group By', 'company:Meta', 'topic:Joins'], 'Show each book with its author name. Show title, author name, and genre.', 'JOIN books → authors on author_id.', 'JOIN authors ON author_id', "SELECT b.title, a.first_name || ' ' || a.last_name AS author_name, b.genre FROM books b JOIN authors a ON b.author_id = a.author_id ORDER BY b.title;", "SELECT b.title, a.first_name || ' ' || a.last_name AS author_name, b.genre FROM books b JOIN authors a ON b.author_id = a.author_id ORDER BY b.title;"), q(422, 'medium', ['Join', 'Group By', 'company:Stripe', 'topic:ER Modeling'], 'Show each member with their total number of loans and any unpaid fines. Show member name, loan_count, unpaid_fines.', 'JOIN members → loans → fines. COUNT loans, SUM unpaid fines.', 'COUNT(loan_id), SUM(CASE WHEN paid_date IS NULL THEN amount ELSE 0 END)', "SELECT m.first_name || ' ' || m.last_name AS member_name, COUNT(DISTINCT l.loan_id) AS loan_count, COALESCE(SUM(CASE WHEN f.paid_date IS NULL THEN f.amount ELSE 0 END), 0) AS unpaid_fines FROM members m LEFT JOIN loans l ON m.member_id = l.member_id LEFT JOIN fines f ON l.loan_id = f.loan_id GROUP BY m.member_id ORDER BY unpaid_fines DESC;", "SELECT m.first_name || ' ' || m.last_name AS member_name, COUNT(DISTINCT l.loan_id) AS loan_count, COALESCE(SUM(CASE WHEN f.paid_date IS NULL THEN f.amount ELSE 0 END), 0) AS unpaid_fines FROM members m LEFT JOIN loans l ON m.member_id = l.member_id LEFT JOIN fines f ON l.loan_id = f.loan_id GROUP BY m.member_id ORDER BY unpaid_fines DESC;"), q(423, 'medium', ['Join', 'Group By', 'company:Stripe', 'topic:Views'], 'Calculate overdue days for each returned loan (return_date - due_date). Show loan_id, book title, overdue_days.', 'julianday(return_date) - julianday(due_date) WHERE return_date > due_date.', 'CAST(julianday(return_date) - julianday(due_date) AS INTEGER) WHERE overdue', 'SELECT l.loan_id, b.title AS book_title, CAST(julianday(l.return_date) - julianday(l.due_date) AS INTEGER) AS overdue_days FROM loans l JOIN books b ON l.book_id = b.book_id WHERE l.return_date > l.due_date ORDER BY overdue_days DESC;', 'SELECT l.loan_id, b.title AS book_title, CAST(julianday(l.return_date) - julianday(l.due_date) AS INTEGER) AS overdue_days FROM loans l JOIN books b ON l.book_id = b.book_id WHERE l.return_date > l.due_date ORDER BY overdue_days DESC;'), q(424, 'medium', ['Join', 'Group By', 'company:NVIDIA', 'topic:Subqueries'], 'Find the most borrowed book. Show title and borrow_count.', 'COUNT(loan_id) GROUP BY book_id, ORDER DESC LIMIT 1.', 'COUNT(loan_id) GROUP BY book_id ORDER BY DESC LIMIT 1', 'SELECT b.title, COUNT(l.loan_id) AS borrow_count FROM books b JOIN loans l ON b.book_id = l.book_id GROUP BY b.book_id ORDER BY borrow_count DESC LIMIT 1;', 'SELECT b.title, COUNT(l.loan_id) AS borrow_count FROM books b JOIN loans l ON b.book_id = l.book_id GROUP BY b.book_id ORDER BY borrow_count DESC LIMIT 1;'), q(425, 'medium', ['Left Join', 'Null Handling', 'company:Salesforce', 'topic:Normalization'], 'Find books that have never been borrowed. Show title, author_id, and genre.', 'LEFT JOIN loans, WHERE loan_id IS NULL.', 'LEFT JOIN loans ON book_id WHERE loan_id IS NULL', 'SELECT b.title, b.author_id, b.genre FROM books b LEFT JOIN loans l ON b.book_id = l.book_id WHERE l.loan_id IS NULL;', 'SELECT b.title, b.author_id, b.genre FROM books b LEFT JOIN loans l ON b.book_id = l.book_id WHERE l.loan_id IS NULL;'), q(426, 'medium', ['Join', 'Group By', 'company:NVIDIA', 'topic:Normalization'], 'Show total number of loans and total fines per branch. Show branch_name, total_loans, total_fines.', 'JOIN branches → members → loans → fines.', 'COUNT(loan_id), SUM(amount) GROUP BY branch', "SELECT br.name AS branch_name, COUNT(l.loan_id) AS total_loans, COALESCE(SUM(f.amount), 0) AS total_fines FROM branches br LEFT JOIN members m ON br.branch_id = m.branch_id LEFT JOIN loans l ON m.member_id = l.member_id LEFT JOIN fines f ON l.loan_id = f.loan_id GROUP BY br.branch_id ORDER BY total_loans DESC;", "SELECT br.name AS branch_name, COUNT(l.loan_id) AS total_loans, COALESCE(SUM(f.amount), 0) AS total_fines FROM branches br LEFT JOIN members m ON br.branch_id = m.branch_id LEFT JOIN loans l ON m.member_id = l.member_id LEFT JOIN fines f ON l.loan_id = f.loan_id GROUP BY br.branch_id ORDER BY total_loans DESC;"), q(427, 'medium', ['Join', 'Group By', 'company:Databricks', 'topic:Views'], 'List authors with more than 3 books. Show author name and book_count.', 'COUNT books per author, HAVING > 3.', 'HAVING COUNT(book_id) > 3', "SELECT a.first_name || ' ' || a.last_name AS author_name, COUNT(b.book_id) AS book_count FROM authors a JOIN books b ON a.author_id = b.author_id GROUP BY a.author_id HAVING COUNT(b.book_id) > 3 ORDER BY book_count DESC;", "SELECT a.first_name || ' ' || a.last_name AS author_name, COUNT(b.book_id) AS book_count FROM authors a JOIN books b ON a.author_id = b.author_id GROUP BY a.author_id HAVING COUNT(b.book_id) > 3 ORDER BY book_count DESC;"), q(428, 'medium', ['Join', 'Date Function', 'company:Uber', 'topic:Joins'], 'Calculate how many days each loan was active (return_date - loan_date). Show loan_id, book_title, and days_borrowed.', 'julianday difference between loan_date and return_date.', 'CAST(julianday(return_date) - julianday(loan_date) AS INTEGER) WHERE return_date IS NOT NULL', 'SELECT l.loan_id, b.title AS book_title, CAST(julianday(l.return_date) - julianday(l.loan_date) AS INTEGER) AS days_borrowed FROM loans l JOIN books b ON l.book_id = b.book_id WHERE l.return_date IS NOT NULL ORDER BY days_borrowed DESC;', 'SELECT l.loan_id, b.title AS book_title, CAST(julianday(l.return_date) - julianday(l.loan_date) AS INTEGER) AS days_borrowed FROM loans l JOIN books b ON l.book_id = b.book_id WHERE l.return_date IS NOT NULL ORDER BY days_borrowed DESC;'), q(429, 'medium', ['Join', 'Group By', 'company:JPMorgan Chase', 'topic:Views'], 'Show member loan activity by month. Show month and loan_count.', 'strftime month from loan_date, COUNT.', 'strftime(\'%Y-%m\', loan_date) GROUP BY month', "SELECT strftime('%Y-%m', loan_date) AS month, COUNT(*) AS loan_count FROM loans GROUP BY month ORDER BY month;", "SELECT strftime('%Y-%m', loan_date) AS month, COUNT(*) AS loan_count FROM loans GROUP BY month ORDER BY month;"), q(430, 'medium', ['Subquery', 'Where', 'company:Amazon', 'topic:Joins'], 'Find members who have borrowed more than the average number of loans per member. Show member name and loan_count.', 'Subquery for average loans, HAVING COUNT > avg.', 'HAVING COUNT(loan_id) > (SELECT AVG(cnt) FROM (SELECT COUNT(*) AS cnt FROM loans GROUP BY member_id))', "SELECT m.first_name || ' ' || m.last_name AS member_name, COUNT(l.loan_id) AS loan_count FROM members m JOIN loans l ON m.member_id = l.member_id GROUP BY m.member_id HAVING COUNT(l.loan_id) > (SELECT AVG(cnt) FROM (SELECT COUNT(*) AS cnt FROM loans GROUP BY member_id)) ORDER BY loan_count DESC;", "SELECT m.first_name || ' ' || m.last_name AS member_name, COUNT(l.loan_id) AS loan_count FROM members m JOIN loans l ON m.member_id = l.member_id GROUP BY m.member_id HAVING COUNT(l.loan_id) > (SELECT AVG(cnt) FROM (SELECT COUNT(*) AS cnt FROM loans GROUP BY member_id)) ORDER BY loan_count DESC;"), q(431, 'medium', ['Join', 'Group By', 'company:Netflix', 'topic:Normalization'], 'Find the most active member per branch (most loans). Show branch_name, member_name, loan_count.', 'JOIN branches → members → loans, GROUP BY member, use subquery or window function for per-branch max.', 'MAX loan_count per branch per member', "SELECT br.name AS branch_name, m.first_name || ' ' || m.last_name AS member_name, COUNT(l.loan_id) AS loan_count FROM branches br JOIN members m ON br.branch_id = m.branch_id JOIN loans l ON m.member_id = l.member_id GROUP BY br.branch_id, m.member_id HAVING COUNT(l.loan_id) = (SELECT MAX(cnt) FROM (SELECT COUNT(*) AS cnt FROM loans l2 JOIN members m2 ON l2.member_id = m2.member_id WHERE m2.branch_id = br.branch_id GROUP BY l2.member_id)) ORDER BY branch_name;", "SELECT br.name AS branch_name, m.first_name || ' ' || m.last_name AS member_name, COUNT(l.loan_id) AS loan_count FROM branches br JOIN members m ON br.branch_id = m.branch_id JOIN loans l ON m.member_id = l.member_id GROUP BY br.branch_id, m.member_id HAVING COUNT(l.loan_id) = (SELECT MAX(cnt) FROM (SELECT COUNT(*) AS cnt FROM loans l2 JOIN members m2 ON l2.member_id = m2.member_id WHERE m2.branch_id = br.branch_id GROUP BY l2.member_id)) ORDER BY branch_name;"), q(432, 'medium', ['Join', 'Group By', 'company:Amazon', 'topic:ER Modeling'], 'Calculate fine collection rate per member: paid fines / total fines. Show member name and collection_rate_pct.', 'SUM(CASE WHEN paid_date IS NOT NULL) / COUNT(*) * 100.', "SUM(CASE WHEN paid_date IS NOT NULL THEN 1 ELSE 0 END) / COUNT(*) * 100", 'SELECT m.first_name || \' \' || m.last_name AS member_name, ROUND(100.0 * SUM(CASE WHEN f.paid_date IS NOT NULL THEN 1 ELSE 0 END) / COUNT(f.fine_id), 1) AS collection_rate_pct FROM members m JOIN loans l ON m.member_id = l.member_id JOIN fines f ON l.loan_id = f.loan_id GROUP BY m.member_id ORDER BY collection_rate_pct DESC;', 'SELECT m.first_name || \' \' || m.last_name AS member_name, ROUND(100.0 * SUM(CASE WHEN f.paid_date IS NOT NULL THEN 1 ELSE 0 END) / COUNT(f.fine_id), 1) AS collection_rate_pct FROM members m JOIN loans l ON m.member_id = l.member_id JOIN fines f ON l.loan_id = f.loan_id GROUP BY m.member_id ORDER BY collection_rate_pct DESC;'), q(433, 'medium', ['Join', 'Group By', 'company:Meta', 'topic:Subqueries'], 'Show the average loan duration (days) per genre. Show genre and avg_days.', 'JOIN loans → books, AVG(return - loan) WHERE returned, GROUP BY genre.', 'AVG(julianday(return_date) - julianday(loan_date)) GROUP BY genre', 'SELECT b.genre, ROUND(AVG(julianday(l.return_date) - julianday(l.loan_date)), 1) AS avg_days FROM books b JOIN loans l ON b.book_id = l.book_id WHERE l.return_date IS NOT NULL GROUP BY b.genre ORDER BY avg_days DESC;', 'SELECT b.genre, ROUND(AVG(julianday(l.return_date) - julianday(l.loan_date)), 1) AS avg_days FROM books b JOIN loans l ON b.book_id = l.book_id WHERE l.return_date IS NOT NULL GROUP BY b.genre ORDER BY avg_days DESC;'), q(434, 'medium', ['Join', 'Group By', 'company:Oracle', 'topic:Normalization'], 'Find the top 3 most borrowed genres. Show genre and borrow_count.', 'JOIN loans → books, COUNT GROUP BY genre, ORDER DESC LIMIT 3.', 'COUNT(loan_id) GROUP BY genre ORDER BY COUNT DESC LIMIT 3', 'SELECT b.genre, COUNT(l.loan_id) AS borrow_count FROM books b JOIN loans l ON b.book_id = l.book_id GROUP BY b.genre ORDER BY borrow_count DESC LIMIT 3;', 'SELECT b.genre, COUNT(l.loan_id) AS borrow_count FROM books b JOIN loans l ON b.book_id = l.book_id GROUP BY b.genre ORDER BY borrow_count DESC LIMIT 3;'), q(435, 'medium', ['Join', 'company:Databricks', 'topic:Execution Order'], 'Show members who have borrowed more than one copy of the same book. Show member_name and book_title.', 'GROUP BY member + book_id HAVING COUNT > 1.', 'HAVING COUNT(loan_id) > 1 GROUP BY member_id, book_id', "SELECT m.first_name || ' ' || m.last_name AS member_name, b.title AS book_title, COUNT(*) AS times_borrowed FROM loans l JOIN members m ON l.member_id = m.member_id JOIN books b ON l.book_id = b.book_id GROUP BY l.member_id, l.book_id HAVING COUNT(*) > 1 ORDER BY times_borrowed DESC;", "SELECT m.first_name || ' ' || m.last_name AS member_name, b.title AS book_title, COUNT(*) AS times_borrowed FROM loans l JOIN members m ON l.member_id = m.member_id JOIN books b ON l.book_id = b.book_id GROUP BY l.member_id, l.book_id HAVING COUNT(*) > 1 ORDER BY times_borrowed DESC;"), q(436, 'medium', ['Join', 'Group By', 'company:Goldman Sachs', 'topic:Execution Order'], 'Show the total unpaid fine amount per branch. Show branch_name and unpaid_total.', 'JOIN branches → members → loans → fines WHERE paid_date IS NULL.', 'SUM(amount) WHERE paid_date IS NULL GROUP BY branch', "SELECT br.name AS branch_name, SUM(f.amount) AS unpaid_total FROM branches br JOIN members m ON br.branch_id = m.branch_id JOIN loans l ON m.member_id = l.member_id JOIN fines f ON l.loan_id = f.loan_id WHERE f.paid_date IS NULL GROUP BY br.branch_id ORDER BY unpaid_total DESC;", "SELECT br.name AS branch_name, SUM(f.amount) AS unpaid_total FROM branches br JOIN members m ON br.branch_id = m.branch_id JOIN loans l ON m.member_id = l.member_id JOIN fines f ON l.loan_id = f.loan_id WHERE f.paid_date IS NULL GROUP BY br.branch_id ORDER BY unpaid_total DESC;"), q(437, 'medium', ['Join', 'Group By', 'company:Infosys', 'topic:Subqueries'], 'Show the overdue return rate per genre: overdue loans / total loans × 100. Show genre and overdue_rate_pct.', 'JOIN loans → books, COUNT and CASE for overdue, GROUP BY genre.', 'SUM(CASE WHEN return_date > due_date THEN 1) / COUNT(*) * 100', 'SELECT b.genre, ROUND(100.0 * SUM(CASE WHEN l.return_date > l.due_date THEN 1 ELSE 0 END) / COUNT(l.loan_id), 1) AS overdue_rate_pct FROM books b JOIN loans l ON b.book_id = l.book_id WHERE l.return_date IS NOT NULL GROUP BY b.genre ORDER BY overdue_rate_pct DESC;', 'SELECT b.genre, ROUND(100.0 * SUM(CASE WHEN l.return_date > l.due_date THEN 1 ELSE 0 END) / COUNT(l.loan_id), 1) AS overdue_rate_pct FROM books b JOIN loans l ON b.book_id = l.book_id WHERE l.return_date IS NOT NULL GROUP BY b.genre ORDER BY overdue_rate_pct DESC;'), q(438, 'medium', ['Join', 'Group By', 'company:Apple', 'topic:Subqueries'], 'Show the number of books currently on loan per branch. Show branch_name and on_loan_count.', 'Loans WHERE return_date IS NULL, JOIN to members for branch.', 'WHERE return_date IS NULL GROUP BY branch_id', "SELECT br.name AS branch_name, COUNT(l.loan_id) AS on_loan_count FROM branches br JOIN members m ON br.branch_id = m.branch_id JOIN loans l ON m.member_id = l.member_id WHERE l.return_date IS NULL GROUP BY br.branch_id ORDER BY on_loan_count DESC;", "SELECT br.name AS branch_name, COUNT(l.loan_id) AS on_loan_count FROM branches br JOIN members m ON br.branch_id = m.branch_id JOIN loans l ON m.member_id = l.member_id WHERE l.return_date IS NULL GROUP BY br.branch_id ORDER BY on_loan_count DESC;"), q(439, 'medium', ['Join', 'Group By', 'company:Databricks', 'topic:ER Modeling'], 'Find the author whose books are borrowed most frequently. Show author name and total_borrows.', 'JOIN books → loans, COUNT, GROUP BY author.', 'COUNT(loan_id) GROUP BY author_id ORDER BY COUNT DESC LIMIT 1', "SELECT a.first_name || ' ' || a.last_name AS author_name, COUNT(l.loan_id) AS total_borrows FROM authors a JOIN books b ON a.author_id = b.author_id JOIN loans l ON b.book_id = l.book_id GROUP BY a.author_id ORDER BY total_borrows DESC LIMIT 1;", "SELECT a.first_name || ' ' || a.last_name AS author_name, COUNT(l.loan_id) AS total_borrows FROM authors a JOIN books b ON a.author_id = b.author_id JOIN loans l ON b.book_id = l.book_id GROUP BY a.author_id ORDER BY total_borrows DESC LIMIT 1;"), q(440, 'medium', ['Join', 'company:Apple', 'topic:Views'], 'Show each loan with fine amount (if any). Show loan_id, book_title, fine_amount (0 if no fine).', 'LEFT JOIN fines, COALESCE(amount, 0).', 'LEFT JOIN fines ON loan_id COALESCE(amount, 0)', 'SELECT l.loan_id, b.title AS book_title, COALESCE(f.amount, 0) AS fine_amount FROM loans l JOIN books b ON l.book_id = b.book_id LEFT JOIN fines f ON l.loan_id = f.loan_id ORDER BY fine_amount DESC;', 'SELECT l.loan_id, b.title AS book_title, COALESCE(f.amount, 0) AS fine_amount FROM loans l JOIN books b ON l.book_id = b.book_id LEFT JOIN fines f ON l.loan_id = f.loan_id ORDER BY fine_amount DESC;'), q(441, 'medium', ['Join', 'Group By', 'company:Meta', 'topic:Execution Order'], 'Calculate borrowing frequency per member per month: average loans per month. Show member name and avg_monthly_loans.', 'COUNT loans / COUNT distinct months per member.', 'COUNT(loan_id) / COUNT(DISTINCT strftime month) GROUP BY member_id', "SELECT m.first_name || ' ' || m.last_name AS member_name, ROUND(COUNT(l.loan_id) * 1.0 / COUNT(DISTINCT strftime('%Y-%m', l.loan_date)), 2) AS avg_monthly_loans FROM members m JOIN loans l ON m.member_id = l.member_id GROUP BY m.member_id ORDER BY avg_monthly_loans DESC;", "SELECT m.first_name || ' ' || m.last_name AS member_name, ROUND(COUNT(l.loan_id) * 1.0 / COUNT(DISTINCT strftime('%Y-%m', l.loan_date)), 2) AS avg_monthly_loans FROM members m JOIN loans l ON m.member_id = l.member_id GROUP BY m.member_id ORDER BY avg_monthly_loans DESC;"), q(442, 'medium', ['Join', 'Group By', 'company:Oracle', 'topic:Subqueries'], 'Show which branch has the highest fine collection rate (paid / total × 100). Show branch_name and collection_rate_pct.', 'JOIN branches → members → loans → fines, SUM paid / COUNT total × 100.', 'SUM(CASE WHEN paid_date IS NOT NULL) / COUNT(*) * 100 GROUP BY branch', 'SELECT br.name AS branch_name, ROUND(100.0 * SUM(CASE WHEN f.paid_date IS NOT NULL THEN 1 ELSE 0 END) / COUNT(f.fine_id), 1) AS collection_rate_pct FROM branches br JOIN members m ON br.branch_id = m.branch_id JOIN loans l ON m.member_id = l.member_id JOIN fines f ON l.loan_id = f.loan_id GROUP BY br.branch_id ORDER BY collection_rate_pct DESC;', 'SELECT br.name AS branch_name, ROUND(100.0 * SUM(CASE WHEN f.paid_date IS NOT NULL THEN 1 ELSE 0 END) / COUNT(f.fine_id), 1) AS collection_rate_pct FROM branches br JOIN members m ON br.branch_id = m.branch_id JOIN loans l ON m.member_id = l.member_id JOIN fines f ON l.loan_id = f.loan_id GROUP BY br.branch_id ORDER BY collection_rate_pct DESC;'), q(443, 'medium', ['Join', 'Group By', 'company:Apple', 'topic:ER Modeling'], 'Show the top 5 members by total fine amount incurred. Show member name and total_fines.', 'JOIN members → loans → fines, SUM(amount) GROUP BY member, ORDER DESC LIMIT 5.', 'SUM(amount) GROUP BY member_id ORDER BY SUM DESC LIMIT 5', "SELECT m.first_name || ' ' || m.last_name AS member_name, SUM(f.amount) AS total_fines FROM members m JOIN loans l ON m.member_id = l.member_id JOIN fines f ON l.loan_id = f.loan_id GROUP BY m.member_id ORDER BY total_fines DESC LIMIT 5;", "SELECT m.first_name || ' ' || m.last_name AS member_name, SUM(f.amount) AS total_fines FROM members m JOIN loans l ON m.member_id = l.member_id JOIN fines f ON l.loan_id = f.loan_id GROUP BY m.member_id ORDER BY total_fines DESC LIMIT 5;"), q(444, 'medium', ['Join', 'Group By', 'company:ServiceNow', 'topic:ER Modeling'], 'Show the monthly borrowing trend for 2024. Show month and loan_count.', 'WHERE 2024 loans, strftime month GROUP BY.', 'WHERE strftime(\'%Y\', loan_date) = \'2024\' GROUP BY month', "SELECT strftime('%m', loan_date) AS month, COUNT(*) AS loan_count FROM loans WHERE strftime('%Y', loan_date) = '2024' GROUP BY month ORDER BY month;", "SELECT strftime('%m', loan_date) AS month, COUNT(*) AS loan_count FROM loans WHERE strftime('%Y', loan_date) = '2024' GROUP BY month ORDER BY month;"), q(445, 'medium', ['Join', 'Group By', 'company:Oracle', 'topic:Joins'], 'Find genres where the average book length exceeds 400 pages. Show genre and avg_pages.', 'AVG(pages) GROUP BY genre HAVING > 400.', 'HAVING AVG(pages) > 400', 'SELECT genre, ROUND(AVG(pages), 0) AS avg_pages FROM books GROUP BY genre HAVING AVG(pages) > 400 ORDER BY avg_pages DESC;', 'SELECT genre, ROUND(AVG(pages), 0) AS avg_pages FROM books GROUP BY genre HAVING AVG(pages) > 400 ORDER BY avg_pages DESC;'),
// HARD (15)
q(446, 'hard', ['Window Function', 'Rank', 'company:Salesforce', 'topic:Locks'], 'Rank books by borrow frequency using DENSE_RANK. Show title, genre, borrow_count, and borrow_rank.', 'DENSE_RANK() OVER (ORDER BY borrow_count DESC).', 'DENSE_RANK() OVER (ORDER BY borrow_count DESC) AS borrow_rank', 'SELECT title, genre, borrow_count, DENSE_RANK() OVER (ORDER BY borrow_count DESC) AS borrow_rank FROM (SELECT b.title, b.genre, COUNT(l.loan_id) AS borrow_count FROM books b LEFT JOIN loans l ON b.book_id = l.book_id GROUP BY b.book_id) ORDER BY borrow_rank;', 'SELECT title, genre, borrow_count, DENSE_RANK() OVER (ORDER BY borrow_count DESC) AS borrow_rank FROM (SELECT b.title, b.genre, COUNT(l.loan_id) AS borrow_count FROM books b LEFT JOIN loans l ON b.book_id = l.book_id GROUP BY b.book_id) ORDER BY borrow_rank;'), q(447, 'hard', ['Window Function', 'Row Number', 'company:Oracle', 'topic:Window Functions'], 'Find the most borrowed book per genre using ROW_NUMBER(). Show genre, title, borrow_count where rn=1.', 'ROW_NUMBER() OVER (PARTITION BY genre ORDER BY borrow_count DESC) WHERE rn=1.', 'ROW_NUMBER() OVER (PARTITION BY genre ORDER BY borrow_count DESC) AS rn WHERE rn=1', 'SELECT genre, title, borrow_count FROM (SELECT b.genre, b.title, COUNT(l.loan_id) AS borrow_count, ROW_NUMBER() OVER (PARTITION BY b.genre ORDER BY COUNT(l.loan_id) DESC) AS rn FROM books b LEFT JOIN loans l ON b.book_id = l.book_id GROUP BY b.book_id) WHERE rn = 1 ORDER BY borrow_count DESC;', 'SELECT genre, title, borrow_count FROM (SELECT b.genre, b.title, COUNT(l.loan_id) AS borrow_count, ROW_NUMBER() OVER (PARTITION BY b.genre ORDER BY COUNT(l.loan_id) DESC) AS rn FROM books b LEFT JOIN loans l ON b.book_id = l.book_id GROUP BY b.book_id) WHERE rn = 1 ORDER BY borrow_count DESC;'), q(448, 'hard', ['Window Function', 'Lag', 'company:Oracle', 'topic:CTEs'], 'Show the running total of fines collected by month. Show month, monthly_fines, and running_total.', 'SUM monthly fines, then SUM OVER (ORDER BY month ROWS UNBOUNDED PRECEDING).', 'SUM(monthly_fines) OVER (ORDER BY month ROWS UNBOUNDED PRECEDING)', "WITH monthly AS (SELECT strftime('%Y-%m', paid_date) AS month, SUM(amount) AS monthly_fines FROM fines WHERE paid_date IS NOT NULL GROUP BY month) SELECT month, monthly_fines, SUM(monthly_fines) OVER (ORDER BY month ROWS UNBOUNDED PRECEDING) AS running_total FROM monthly ORDER BY month;", "WITH monthly AS (SELECT strftime('%Y-%m', paid_date) AS month, SUM(amount) AS monthly_fines FROM fines WHERE paid_date IS NOT NULL GROUP BY month) SELECT month, monthly_fines, SUM(monthly_fines) OVER (ORDER BY month ROWS UNBOUNDED PRECEDING) AS running_total FROM monthly ORDER BY month;"), q(449, 'hard', ['CTE', 'Window Function', 'company:Microsoft', 'topic:Transactions'], 'Calculate each member\'s first loan and how many days ago that was. Show member_name, first_loan_date, and days_since_first_loan.', 'MIN(loan_date) per member. julianday difference from today (use 2024-04-01 as reference).', 'MIN(loan_date) GROUP BY member_id, julianday diff', "WITH first_loans AS (SELECT member_id, MIN(loan_date) AS first_loan_date FROM loans GROUP BY member_id) SELECT m.first_name || ' ' || m.last_name AS member_name, fl.first_loan_date, CAST(julianday('2024-04-01') - julianday(fl.first_loan_date) AS INTEGER) AS days_since_first_loan FROM members m JOIN first_loans fl ON m.member_id = fl.member_id ORDER BY days_since_first_loan DESC;", "WITH first_loans AS (SELECT member_id, MIN(loan_date) AS first_loan_date FROM loans GROUP BY member_id) SELECT m.first_name || ' ' || m.last_name AS member_name, fl.first_loan_date, CAST(julianday('2024-04-01') - julianday(fl.first_loan_date) AS INTEGER) AS days_since_first_loan FROM members m JOIN first_loans fl ON m.member_id = fl.member_id ORDER BY days_since_first_loan DESC;"), q(450, 'hard', ['Window Function', 'CTE', 'company:Atlassian', 'topic:Locks'], 'Show fine accumulation over time per member: for each fine, show a running total of fines per member sorted by date.', 'SUM(amount) OVER (PARTITION BY member_id ORDER BY loan_date ROWS UNBOUNDED PRECEDING).', 'SUM(f.amount) OVER (PARTITION BY m.member_id ORDER BY l.loan_date)', "SELECT m.first_name || ' ' || m.last_name AS member_name, l.loan_date, f.amount, SUM(f.amount) OVER (PARTITION BY m.member_id ORDER BY l.loan_date ROWS UNBOUNDED PRECEDING) AS running_fine_total FROM members m JOIN loans l ON m.member_id = l.member_id JOIN fines f ON l.loan_id = f.loan_id ORDER BY member_name, l.loan_date;", "SELECT m.first_name || ' ' || m.last_name AS member_name, l.loan_date, f.amount, SUM(f.amount) OVER (PARTITION BY m.member_id ORDER BY l.loan_date ROWS UNBOUNDED PRECEDING) AS running_fine_total FROM members m JOIN loans l ON m.member_id = l.member_id JOIN fines f ON l.loan_id = f.loan_id ORDER BY member_name, l.loan_date;"), q(451, 'hard', ['Window Function', 'Rank', 'company:Infosys', 'topic:Window Functions'], 'Calculate each book\'s borrow share as % of total loans. Show title, borrow_count, and pct_of_total.', 'borrow_count / SUM(borrow_count) OVER () * 100.', 'borrow_count / SUM(borrow_count) OVER () * 100 AS pct_of_total', 'SELECT title, borrow_count, ROUND(100.0 * borrow_count / SUM(borrow_count) OVER (), 2) AS pct_of_total FROM (SELECT b.title, COUNT(l.loan_id) AS borrow_count FROM books b JOIN loans l ON b.book_id = l.book_id GROUP BY b.book_id) ORDER BY borrow_count DESC;', 'SELECT title, borrow_count, ROUND(100.0 * borrow_count / SUM(borrow_count) OVER (), 2) AS pct_of_total FROM (SELECT b.title, COUNT(l.loan_id) AS borrow_count FROM books b JOIN loans l ON b.book_id = l.book_id GROUP BY b.book_id) ORDER BY borrow_count DESC;'), q(452, 'hard', ['Window Function', 'Lead', 'company:Databricks', 'topic:Indexing'], 'For each member, show their previous loan book and next loan book (using LAG and LEAD). Show member_id, current_book, prev_book, next_book.', 'LAG(title) and LEAD(title) OVER (PARTITION BY member_id ORDER BY loan_date).', 'LAG(b.title) OVER (...) AS prev_book, LEAD(b.title) OVER (...) AS next_book', 'SELECT l.member_id, b.title AS current_book, LAG(b.title) OVER (PARTITION BY l.member_id ORDER BY l.loan_date) AS prev_book, LEAD(b.title) OVER (PARTITION BY l.member_id ORDER BY l.loan_date) AS next_book FROM loans l JOIN books b ON l.book_id = b.book_id ORDER BY l.member_id, l.loan_date;', 'SELECT l.member_id, b.title AS current_book, LAG(b.title) OVER (PARTITION BY l.member_id ORDER BY l.loan_date) AS prev_book, LEAD(b.title) OVER (PARTITION BY l.member_id ORDER BY l.loan_date) AS next_book FROM loans l JOIN books b ON l.book_id = b.book_id ORDER BY l.member_id, l.loan_date;'), q(453, 'hard', ['CTE', 'Window Function', 'company:Oracle', 'topic:Locks'], 'Find the time between consecutive borrows for each member (days between loans). Show member_id, loan_date, prev_loan_date, and days_between_loans.', 'LAG(loan_date) OVER (PARTITION BY member_id ORDER BY loan_date). julianday diff.', 'julianday(loan_date) - julianday(LAG(loan_date) ...) AS days_between_loans', 'SELECT member_id, loan_date, LAG(loan_date) OVER (PARTITION BY member_id ORDER BY loan_date) AS prev_loan_date, CAST(julianday(loan_date) - julianday(LAG(loan_date) OVER (PARTITION BY member_id ORDER BY loan_date)) AS INTEGER) AS days_between_loans FROM loans ORDER BY member_id, loan_date;', 'SELECT member_id, loan_date, LAG(loan_date) OVER (PARTITION BY member_id ORDER BY loan_date) AS prev_loan_date, CAST(julianday(loan_date) - julianday(LAG(loan_date) OVER (PARTITION BY member_id ORDER BY loan_date)) AS INTEGER) AS days_between_loans FROM loans ORDER BY member_id, loan_date;'), q(454, 'hard', ['Window Function', 'Rank', 'company:Accenture', 'topic:CTEs'], 'Use NTILE(3) to segment members by activity level (loans taken). Show member_name, loan_count, and activity_tier (1=most active, 3=least active).', 'NTILE(3) OVER (ORDER BY loan_count DESC).', 'NTILE(3) OVER (ORDER BY loan_count DESC) AS activity_tier', "SELECT member_name, loan_count, NTILE(3) OVER (ORDER BY loan_count DESC) AS activity_tier FROM (SELECT m.first_name || ' ' || m.last_name AS member_name, COUNT(l.loan_id) AS loan_count FROM members m LEFT JOIN loans l ON m.member_id = l.member_id GROUP BY m.member_id) ORDER BY activity_tier, loan_count DESC;", "SELECT member_name, loan_count, NTILE(3) OVER (ORDER BY loan_count DESC) AS activity_tier FROM (SELECT m.first_name || ' ' || m.last_name AS member_name, COUNT(l.loan_id) AS loan_count FROM members m LEFT JOIN loans l ON m.member_id = l.member_id GROUP BY m.member_id) ORDER BY activity_tier, loan_count DESC;"), q(455, 'hard', ['CTE', 'Window Function', 'company:Netflix', 'topic:Indexing'], 'Calculate a 3-month rolling average of library loans. Show month, loan_count, and rolling_avg.', 'SUM loans per month, then AVG OVER (3 months window).', 'AVG(loan_count) OVER (ORDER BY month ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)', "WITH monthly AS (SELECT strftime('%Y-%m', loan_date) AS month, COUNT(*) AS loan_count FROM loans GROUP BY month) SELECT month, loan_count, ROUND(AVG(loan_count) OVER (ORDER BY month ROWS BETWEEN 2 PRECEDING AND CURRENT ROW), 1) AS rolling_avg FROM monthly ORDER BY month;", "WITH monthly AS (SELECT strftime('%Y-%m', loan_date) AS month, COUNT(*) AS loan_count FROM loans GROUP BY month) SELECT month, loan_count, ROUND(AVG(loan_count) OVER (ORDER BY month ROWS BETWEEN 2 PRECEDING AND CURRENT ROW), 1) AS rolling_avg FROM monthly ORDER BY month;"), q(456, 'hard', ['CTE', 'Window Function', 'Rank', 'company:Microsoft', 'topic:Transactions'], 'Build a comprehensive book report: for each book show title, genre, total_borrows, total_fines_generated, and book_score = borrows × 10 - fines × 5. Rank by book_score.', 'Multiple CTEs for borrows and fines per book. Compute score. RANK() DESC.', 'RANK() OVER (ORDER BY book_score DESC)', "WITH borrows AS (SELECT book_id, COUNT(*) AS total_borrows FROM loans GROUP BY book_id), fines_per_book AS (SELECT l.book_id, SUM(f.amount) AS total_fines FROM loans l JOIN fines f ON l.loan_id = f.loan_id GROUP BY l.book_id) SELECT b.title, b.genre, COALESCE(br.total_borrows, 0) AS total_borrows, COALESCE(fp.total_fines, 0) AS total_fines_generated, COALESCE(br.total_borrows, 0) * 10 - COALESCE(fp.total_fines, 0) * 5 AS book_score, RANK() OVER (ORDER BY COALESCE(br.total_borrows, 0) * 10 - COALESCE(fp.total_fines, 0) * 5 DESC) AS book_rank FROM books b LEFT JOIN borrows br ON b.book_id = br.book_id LEFT JOIN fines_per_book fp ON b.book_id = fp.book_id ORDER BY book_rank;", "WITH borrows AS (SELECT book_id, COUNT(*) AS total_borrows FROM loans GROUP BY book_id), fines_per_book AS (SELECT l.book_id, SUM(f.amount) AS total_fines FROM loans l JOIN fines f ON l.loan_id = f.loan_id GROUP BY l.book_id) SELECT b.title, b.genre, COALESCE(br.total_borrows, 0) AS total_borrows, COALESCE(fp.total_fines, 0) AS total_fines_generated, COALESCE(br.total_borrows, 0) * 10 - COALESCE(fp.total_fines, 0) * 5 AS book_score, RANK() OVER (ORDER BY COALESCE(br.total_borrows, 0) * 10 - COALESCE(fp.total_fines, 0) * 5 DESC) AS book_rank FROM books b LEFT JOIN borrows br ON b.book_id = br.book_id LEFT JOIN fines_per_book fp ON b.book_id = fp.book_id ORDER BY book_rank;"), q(457, 'hard', ['Window Function', 'CTE', 'company:TCS', 'topic:Transactions'], 'Show a year-over-year comparison of library usage (loan counts). Show year, total_loans, prev_year_loans, and yoy_change.', 'GROUP BY year, LAG over year.', 'LAG(total_loans) OVER (ORDER BY year)', "WITH yearly AS (SELECT strftime('%Y', loan_date) AS year, COUNT(*) AS total_loans FROM loans GROUP BY year) SELECT year, total_loans, LAG(total_loans) OVER (ORDER BY year) AS prev_year_loans, total_loans - LAG(total_loans) OVER (ORDER BY year) AS yoy_change FROM yearly ORDER BY year;", "WITH yearly AS (SELECT strftime('%Y', loan_date) AS year, COUNT(*) AS total_loans FROM loans GROUP BY year) SELECT year, total_loans, LAG(total_loans) OVER (ORDER BY year) AS prev_year_loans, total_loans - LAG(total_loans) OVER (ORDER BY year) AS yoy_change FROM yearly ORDER BY year;"), q(458, 'hard', ['Window Function', 'Rank', 'company:Infosys', 'topic:Transactions'], 'Show each loan\'s overdue days and rank it as a percentile among all overdue loans. Show loan_id, overdue_days, and overdue_percentile.', 'PERCENT_RANK() OVER (ORDER BY overdue_days) for overdue only.', 'ROUND(PERCENT_RANK() OVER (ORDER BY overdue_days) * 100, 1) AS overdue_percentile', 'SELECT loan_id, overdue_days, ROUND(PERCENT_RANK() OVER (ORDER BY overdue_days) * 100, 1) AS overdue_percentile FROM (SELECT loan_id, CAST(julianday(return_date) - julianday(due_date) AS INTEGER) AS overdue_days FROM loans WHERE return_date > due_date) ORDER BY overdue_days DESC;', 'SELECT loan_id, overdue_days, ROUND(PERCENT_RANK() OVER (ORDER BY overdue_days) * 100, 1) AS overdue_percentile FROM (SELECT loan_id, CAST(julianday(return_date) - julianday(due_date) AS INTEGER) AS overdue_days FROM loans WHERE return_date > due_date) ORDER BY overdue_days DESC;'), q(459, 'hard', ['CTE', 'Window Function', 'company:Goldman Sachs', 'topic:Transactions'], 'Identify books that are currently overdue (on loan and past due_date). Show book_title, member_name, due_date, and days_overdue.', 'WHERE return_date IS NULL AND due_date < \'2024-04-01\'. julianday difference.', 'WHERE return_date IS NULL AND due_date < current reference date', "SELECT b.title AS book_title, m.first_name || ' ' || m.last_name AS member_name, l.due_date, CAST(julianday('2024-04-01') - julianday(l.due_date) AS INTEGER) AS days_overdue FROM loans l JOIN books b ON l.book_id = b.book_id JOIN members m ON l.member_id = m.member_id WHERE l.return_date IS NULL AND l.due_date < '2024-04-01' ORDER BY days_overdue DESC;", "SELECT b.title AS book_title, m.first_name || ' ' || m.last_name AS member_name, l.due_date, CAST(julianday('2024-04-01') - julianday(l.due_date) AS INTEGER) AS days_overdue FROM loans l JOIN books b ON l.book_id = b.book_id JOIN members m ON l.member_id = m.member_id WHERE l.return_date IS NULL AND l.due_date < '2024-04-01' ORDER BY days_overdue DESC;"), q(460, 'hard', ['CTE', 'Window Function', 'Rank', 'company:Meta', 'topic:Locks'], 'Build a member loyalty score: total_loans × 5 - total_unpaid_fines × 10. Rank members by loyalty_score. Show member_name, total_loans, unpaid_fines, loyalty_score, and rank.', 'Multiple CTEs. Compute score. RANK() OVER loyalty_score DESC.', 'loyalty_score = total_loans * 5 - unpaid_fines * 10, RANK() DESC', "WITH loan_counts AS (SELECT member_id, COUNT(*) AS total_loans FROM loans GROUP BY member_id), unpaid AS (SELECT l.member_id, SUM(f.amount) AS unpaid_fines FROM loans l JOIN fines f ON l.loan_id = f.loan_id WHERE f.paid_date IS NULL GROUP BY l.member_id) SELECT m.first_name || ' ' || m.last_name AS member_name, COALESCE(lc.total_loans, 0) AS total_loans, COALESCE(u.unpaid_fines, 0) AS unpaid_fines, COALESCE(lc.total_loans, 0) * 5 - COALESCE(u.unpaid_fines, 0) * 10 AS loyalty_score, RANK() OVER (ORDER BY COALESCE(lc.total_loans, 0) * 5 - COALESCE(u.unpaid_fines, 0) * 10 DESC) AS loyalty_rank FROM members m LEFT JOIN loan_counts lc ON m.member_id = lc.member_id LEFT JOIN unpaid u ON m.member_id = u.member_id ORDER BY loyalty_rank;", "WITH loan_counts AS (SELECT member_id, COUNT(*) AS total_loans FROM loans GROUP BY member_id), unpaid AS (SELECT l.member_id, SUM(f.amount) AS unpaid_fines FROM loans l JOIN fines f ON l.loan_id = f.loan_id WHERE f.paid_date IS NULL GROUP BY l.member_id) SELECT m.first_name || ' ' || m.last_name AS member_name, COALESCE(lc.total_loans, 0) AS total_loans, COALESCE(u.unpaid_fines, 0) AS unpaid_fines, COALESCE(lc.total_loans, 0) * 5 - COALESCE(u.unpaid_fines, 0) * 10 AS loyalty_score, RANK() OVER (ORDER BY COALESCE(lc.total_loans, 0) * 5 - COALESCE(u.unpaid_fines, 0) * 10 DESC) AS loyalty_rank FROM members m LEFT JOIN loan_counts lc ON m.member_id = lc.member_id LEFT JOIN unpaid u ON m.member_id = u.member_id ORDER BY loyalty_rank;")];
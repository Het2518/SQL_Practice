const noRows = {
  columns: [],
  rows: []
};
const q = (id, diff, kw, prompt, h1, h2, h3, sql) => ({
  id,
  db: 'university',
  difficulty: diff,
  keywords: kw,
  prompt,
  hint1: h1,
  hint2: h2,
  hint3: h3,
  solutionSQL: sql,
  expectedResult: noRows
});
export const universityQuestions = [q(121, 'easy', ['Select', 'company:Microsoft', 'topic:Aggregate Functions'], 'List all students with their first_name, last_name, and major.', 'SELECT from the students table.', 'SELECT first_name, last_name, major FROM students', 'SELECT first_name, last_name, major FROM students;', 'SELECT first_name, last_name, major FROM students;'), q(122, 'easy', ['Select', 'Where', 'company:Airbnb', 'topic:Aggregate Functions'], "Find all students majoring in 'Computer Science'. Show first_name, last_name.", "Use WHERE major = 'Computer Science'", "WHERE major = 'Computer Science'", "SELECT first_name, last_name FROM students WHERE major = 'Computer Science';", "SELECT first_name, last_name FROM students WHERE major = 'Computer Science';"), q(123, 'easy', ['Select', 'company:Netflix', 'topic:Keys & Constraints'], "List all professors who have tenure (tenure = 1). Show first_name, last_name, and email.", 'Filter where tenure = 1.', 'WHERE tenure = 1', 'SELECT first_name, last_name, email FROM professors WHERE tenure = 1;', 'SELECT first_name, last_name, email FROM professors WHERE tenure = 1;'), q(124, 'easy', ['Select', 'Order By', 'company:Databricks', 'topic:Keys & Constraints'], 'List all courses ordered by credit_hours descending. Show title and credit_hours.', 'ORDER BY credit_hours DESC', 'ORDER BY credit_hours DESC', 'SELECT title, credit_hours FROM courses ORDER BY credit_hours DESC;', 'SELECT title, credit_hours FROM courses ORDER BY credit_hours DESC;'), q(125, 'easy', ['Select', 'Where', 'company:Oracle', 'topic:Database Design'], 'Find all classrooms with a capacity greater than 50. Show building, room_number, capacity.', 'WHERE capacity > 50', 'WHERE capacity > 50 ORDER BY capacity DESC', 'SELECT building, room_number, capacity FROM classrooms WHERE capacity > 50 ORDER BY capacity DESC;', 'SELECT building, room_number, capacity FROM classrooms WHERE capacity > 50 ORDER BY capacity DESC;'), q(126, 'easy', ['Select', 'Group By', 'company:NVIDIA', 'topic:Database Design'], "Count the number of students per major. Show major and student_count, ordered by count DESC.", 'GROUP BY major COUNT(*)', 'SELECT major, COUNT(*) AS student_count FROM students GROUP BY major ORDER BY student_count DESC', 'SELECT major, COUNT(*) AS student_count FROM students GROUP BY major ORDER BY student_count DESC;', 'SELECT major, COUNT(*) AS student_count FROM students GROUP BY major ORDER BY student_count DESC;'), q(127, 'easy', ['Select', 'Where', 'company:Microsoft', 'topic:Database Design'], "Show all grades with letter_grade = 'A'. Show grade_id, enrollment_id, final_score.", 'WHERE letter_grade = \'A\'', 'WHERE letter_grade = \'A\'', 'SELECT grade_id, enrollment_id, final_score FROM grades WHERE letter_grade = \'A\';', 'SELECT grade_id, enrollment_id, final_score FROM grades WHERE letter_grade = \'A\';'), q(128, 'easy', ['Select', 'company:Atlassian', 'topic:Aggregate Functions'], 'List all semesters with their name, start_date, and end_date, ordered by start_date.', 'ORDER BY start_date ASC', 'SELECT name, start_date, end_date FROM semesters ORDER BY start_date', 'SELECT name, start_date, end_date FROM semesters ORDER BY start_date;', 'SELECT name, start_date, end_date FROM semesters ORDER BY start_date;'), q(129, 'easy', ['Select', 'Where', 'company:Netflix', 'topic:Basic SQL'], 'Find courses worth 4 credit hours. Show course_id and title.', 'WHERE credit_hours = 4', 'WHERE credit_hours = 4', 'SELECT course_id, title FROM courses WHERE credit_hours = 4;', 'SELECT course_id, title FROM courses WHERE credit_hours = 4;'), q(130, 'easy', ['Select', 'company:Amazon', 'topic:Aggregate Functions'], 'Count how many professors are in the database. Return a single number labeled professor_count.', 'COUNT(*) on professors', 'SELECT COUNT(*) AS professor_count FROM professors', 'SELECT COUNT(*) AS professor_count FROM professors;', 'SELECT COUNT(*) AS professor_count FROM professors;'), q(131, 'easy', ['Select', 'Where', 'Null Handling', 'company:Meta', 'topic:Basic SQL'], 'Find students with no advisor assigned (advisor_id IS NULL). Show first_name, last_name, major.', 'IS NULL check on advisor_id.', 'WHERE advisor_id IS NULL', 'SELECT first_name, last_name, major FROM students WHERE advisor_id IS NULL;', 'SELECT first_name, last_name, major FROM students WHERE advisor_id IS NULL;'), q(132, 'easy', ['Select', 'Join', 'company:Stripe', 'topic:Aggregate Functions'], "Show courses and their department name. Show title, credit_hours, and department name.", 'JOIN courses to departments on department_id.', 'JOIN departments ON department_id', 'SELECT c.title, c.credit_hours, d.name AS department_name FROM courses c JOIN departments d ON c.department_id = d.department_id;', 'SELECT c.title, c.credit_hours, d.name AS department_name FROM courses c JOIN departments d ON c.department_id = d.department_id;'), q(133, 'easy', ['Select', 'Where', 'Date Function', 'company:Accenture', 'topic:Basic SQL'], "Find students who enrolled before 2022. Show first_name, last_name, enrolled_since.", "strftime or comparison < '2022-01-01'", "WHERE enrolled_since < '2022-01-01'", "SELECT first_name, last_name, enrolled_since FROM students WHERE enrolled_since < '2022-01-01';", "SELECT first_name, last_name, enrolled_since FROM students WHERE enrolled_since < '2022-01-01';"), q(134, 'easy', ['Select', 'Group By', 'company:Salesforce', 'topic:Aggregate Functions'], 'Count enrollments per semester. Show semester_id and enrollment_count.', 'GROUP BY semester_id', 'SELECT semester_id, COUNT(*) AS enrollment_count FROM enrollments GROUP BY semester_id', 'SELECT semester_id, COUNT(*) AS enrollment_count FROM enrollments GROUP BY semester_id ORDER BY semester_id;', 'SELECT semester_id, COUNT(*) AS enrollment_count FROM enrollments GROUP BY semester_id ORDER BY semester_id;'), q(135, 'easy', ['Select', 'Order By', 'company:JPMorgan Chase', 'topic:Aggregate Functions'], 'Show the top 5 professors by earliest hire date (most senior). Show first_name, last_name, hired_at.', 'ORDER BY hired_at ASC LIMIT 5', 'ORDER BY hired_at ASC LIMIT 5', 'SELECT first_name, last_name, hired_at FROM professors ORDER BY hired_at ASC LIMIT 5;', 'SELECT first_name, last_name, hired_at FROM professors ORDER BY hired_at ASC LIMIT 5;'), q(136, 'easy', ['Select', 'Where', 'company:Airbnb', 'topic:Database Design'], "List all courses in the Computer Science department (department_id = 1). Show title.", 'WHERE department_id = 1', 'WHERE department_id = 1', 'SELECT title FROM courses WHERE department_id = 1;', 'SELECT title FROM courses WHERE department_id = 1;'), q(137, 'easy', ['Select', 'company:Apple', 'topic:Database Design'], 'Show all departments with their name and building.', 'Simple SELECT from departments.', 'SELECT name, building FROM departments', 'SELECT name, building FROM departments;', 'SELECT name, building FROM departments;'), q(138, 'easy', ['Select', 'Group By', 'company:Accenture', 'topic:Database Design'], 'Count how many courses each department offers. Show department_id and course_count.', 'GROUP BY department_id', 'SELECT department_id, COUNT(*) AS course_count FROM courses GROUP BY department_id', 'SELECT department_id, COUNT(*) AS course_count FROM courses GROUP BY department_id ORDER BY course_count DESC;', 'SELECT department_id, COUNT(*) AS course_count FROM courses GROUP BY department_id ORDER BY course_count DESC;'), q(139, 'easy', ['Select', 'Where', 'company:Microsoft', 'topic:Database Design'], 'Find all grades with final_score above 90. Show grade_id, final_score, letter_grade.', 'WHERE final_score > 90', 'WHERE final_score > 90 ORDER BY final_score DESC', 'SELECT grade_id, final_score, letter_grade FROM grades WHERE final_score > 90 ORDER BY final_score DESC;', 'SELECT grade_id, final_score, letter_grade FROM grades WHERE final_score > 90 ORDER BY final_score DESC;'), q(140, 'easy', ['Select', 'Where', 'company:Google', 'topic:Aggregate Functions'], 'Find all courses that have a prerequisite (prereq_course_id IS NOT NULL). Show title and prereq_course_id.', 'WHERE prereq_course_id IS NOT NULL', 'WHERE prereq_course_id IS NOT NULL', 'SELECT title, prereq_course_id FROM courses WHERE prereq_course_id IS NOT NULL;', 'SELECT title, prereq_course_id FROM courses WHERE prereq_course_id IS NOT NULL;'),
// MEDIUM
q(141, 'medium', ['Join', 'Group By', 'company:Infosys', 'topic:Subqueries'], 'Calculate the average grade score per course. Show course title and avg_score. Order by avg_score DESC.', 'JOIN enrollments → grades, GROUP BY course, AVG(final_score).', 'JOIN grades ON enrollment_id GROUP BY course_id', 'SELECT c.title, ROUND(AVG(g.final_score), 2) AS avg_score FROM enrollments e JOIN grades g ON e.enrollment_id = g.grade_id JOIN courses c ON e.course_id = c.course_id GROUP BY c.course_id, c.title ORDER BY avg_score DESC;', 'SELECT c.title, ROUND(AVG(g.final_score), 2) AS avg_score FROM enrollments e JOIN grades g ON e.enrollment_id = g.grade_id JOIN courses c ON e.course_id = c.course_id GROUP BY c.course_id, c.title ORDER BY avg_score DESC;'), q(142, 'medium', ['Join', 'Group By', 'company:Netflix', 'topic:Views'], 'Show the student with the highest average final score across all their enrolled courses. Show student name and avg_score.', 'JOIN students → enrollments → grades, GROUP BY student, ORDER BY AVG DESC LIMIT 1.', 'AVG(g.final_score) GROUP BY student_id ORDER BY avg DESC LIMIT 1', "SELECT s.first_name || ' ' || s.last_name AS student_name, ROUND(AVG(g.final_score), 2) AS avg_score FROM students s JOIN enrollments e ON s.student_id = e.student_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY s.student_id ORDER BY avg_score DESC LIMIT 1;", "SELECT s.first_name || ' ' || s.last_name AS student_name, ROUND(AVG(g.final_score), 2) AS avg_score FROM students s JOIN enrollments e ON s.student_id = e.student_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY s.student_id ORDER BY avg_score DESC LIMIT 1;"), q(143, 'medium', ['Join', 'Group By', 'Having', 'company:Apple', 'topic:Joins'], "Find departments with more than 5 courses. Show department name and course_count.", 'JOIN courses to departments, GROUP BY, HAVING COUNT > 5.', 'HAVING COUNT(course_id) > 5', 'SELECT d.name, COUNT(c.course_id) AS course_count FROM departments d JOIN courses c ON d.department_id = c.department_id GROUP BY d.department_id HAVING COUNT(c.course_id) > 5;', 'SELECT d.name, COUNT(c.course_id) AS course_count FROM departments d JOIN courses c ON d.department_id = c.department_id GROUP BY d.department_id HAVING COUNT(c.course_id) > 5;'), q(144, 'medium', ['Join', 'Left Join', 'company:Stripe', 'topic:Normalization'], 'List professors who have no students assigned to them as advisor. Show first_name, last_name.', 'LEFT JOIN students ON advisor_id = professor_id, WHERE student_id IS NULL.', 'LEFT JOIN students ON advisor_id WHERE student_id IS NULL', 'SELECT p.first_name, p.last_name FROM professors p LEFT JOIN students s ON p.professor_id = s.advisor_id WHERE s.student_id IS NULL;', 'SELECT p.first_name, p.last_name FROM professors p LEFT JOIN students s ON p.professor_id = s.advisor_id WHERE s.student_id IS NULL;'), q(145, 'medium', ['Join', 'Group By', 'company:Goldman Sachs', 'topic:Normalization'], 'Count how many students are enrolled in each course. Show course title and student_count.', 'JOIN enrollments to courses, COUNT, GROUP BY.', 'COUNT(student_id) GROUP BY course_id', 'SELECT c.title, COUNT(e.student_id) AS student_count FROM courses c JOIN enrollments e ON c.course_id = e.course_id GROUP BY c.course_id, c.title ORDER BY student_count DESC;', 'SELECT c.title, COUNT(e.student_id) AS student_count FROM courses c JOIN enrollments e ON c.course_id = e.course_id GROUP BY c.course_id, c.title ORDER BY student_count DESC;'), q(146, 'medium', ['Subquery', 'Where', 'company:NVIDIA', 'topic:Views'], 'Find students who have enrolled in more courses than the average number of enrollments per student. Show first_name, last_name, course_count.', 'Use subquery for avg enrollment count, filter WHERE count > avg.', 'WHERE count > (SELECT AVG(cnt) FROM (SELECT COUNT(*) AS cnt FROM enrollments GROUP BY student_id))', 'SELECT s.first_name, s.last_name, COUNT(e.enrollment_id) AS course_count FROM students s JOIN enrollments e ON s.student_id = e.student_id GROUP BY s.student_id HAVING COUNT(e.enrollment_id) > (SELECT AVG(cnt) FROM (SELECT COUNT(*) AS cnt FROM enrollments GROUP BY student_id));', 'SELECT s.first_name, s.last_name, COUNT(e.enrollment_id) AS course_count FROM students s JOIN enrollments e ON s.student_id = e.student_id GROUP BY s.student_id HAVING COUNT(e.enrollment_id) > (SELECT AVG(cnt) FROM (SELECT COUNT(*) AS cnt FROM enrollments GROUP BY student_id));'), q(147, 'medium', ['Join', 'company:Databricks', 'topic:Joins'], 'Show each student their advisor name. Show student name and advisor name.', 'JOIN students to professors on advisor_id.', 'JOIN professors ON advisor_id = professor_id', "SELECT s.first_name || ' ' || s.last_name AS student_name, p.first_name || ' ' || p.last_name AS advisor_name FROM students s LEFT JOIN professors p ON s.advisor_id = p.professor_id ORDER BY student_name;", "SELECT s.first_name || ' ' || s.last_name AS student_name, p.first_name || ' ' || p.last_name AS advisor_name FROM students s LEFT JOIN professors p ON s.advisor_id = p.professor_id ORDER BY student_name;"), q(148, 'medium', ['Join', 'Group By', 'company:Microsoft', 'topic:Normalization'], 'Find departments with the highest average course credit_hours. Show department_name and avg_credits.', 'AVG(credit_hours) GROUP BY department.', 'SELECT d.name, AVG(c.credit_hours) FROM courses c JOIN departments d GROUP BY d.name', 'SELECT d.name AS department_name, ROUND(AVG(c.credit_hours), 2) AS avg_credits FROM departments d JOIN courses c ON d.department_id = c.department_id GROUP BY d.department_id, d.name ORDER BY avg_credits DESC;', 'SELECT d.name AS department_name, ROUND(AVG(c.credit_hours), 2) AS avg_credits FROM departments d JOIN courses c ON d.department_id = c.department_id GROUP BY d.department_id, d.name ORDER BY avg_credits DESC;'), q(149, 'medium', ['Join', 'Group By', 'company:Airbnb', 'topic:Execution Order'], 'Show the grade distribution (count per letter grade) for all students. Show letter_grade and count.', 'GROUP BY letter_grade, COUNT(*).', 'SELECT letter_grade, COUNT(*) FROM grades GROUP BY letter_grade', 'SELECT letter_grade, COUNT(*) AS grade_count FROM grades GROUP BY letter_grade ORDER BY letter_grade;', 'SELECT letter_grade, COUNT(*) AS grade_count FROM grades GROUP BY letter_grade ORDER BY letter_grade;'), q(150, 'medium', ['Join', 'Group By', 'company:Oracle', 'topic:Subqueries'], 'Find the semester with the most enrollments. Show semester name and enrollment_count.', 'JOIN semesters to enrollments, GROUP BY, ORDER DESC LIMIT 1.', 'COUNT(*) GROUP BY semester_id ORDER BY COUNT(*) DESC LIMIT 1', 'SELECT s.name AS semester_name, COUNT(e.enrollment_id) AS enrollment_count FROM semesters s JOIN enrollments e ON s.semester_id = e.semester_id GROUP BY s.semester_id, s.name ORDER BY enrollment_count DESC LIMIT 1;', 'SELECT s.name AS semester_name, COUNT(e.enrollment_id) AS enrollment_count FROM semesters s JOIN enrollments e ON s.semester_id = e.semester_id GROUP BY s.semester_id, s.name ORDER BY enrollment_count DESC LIMIT 1;'), q(151, 'medium', ['Join', 'Group By', 'company:Atlassian', 'topic:Joins'], 'List all professors with the number of students they advise. Show professor name and advisee_count.', 'LEFT JOIN students ON advisor_id, COUNT.', 'LEFT JOIN students ON advisor_id COUNT GROUP BY professor', "SELECT p.first_name || ' ' || p.last_name AS professor_name, COUNT(s.student_id) AS advisee_count FROM professors p LEFT JOIN students s ON p.professor_id = s.advisor_id GROUP BY p.professor_id ORDER BY advisee_count DESC;", "SELECT p.first_name || ' ' || p.last_name AS professor_name, COUNT(s.student_id) AS advisee_count FROM professors p LEFT JOIN students s ON p.professor_id = s.advisor_id GROUP BY p.professor_id ORDER BY advisee_count DESC;"), q(152, 'medium', ['Join', 'Group By', 'company:Netflix', 'topic:Joins'], "Calculate average midterm_score and average final_score per department. Show department_name, avg_midterm, avg_final.", 'Chain JOIN enrollments→grades, JOIN to courses, JOIN to departments. AVG both scores.', 'AVG(midterm_score), AVG(final_score) GROUP BY department', 'SELECT d.name AS department_name, ROUND(AVG(g.midterm_score), 2) AS avg_midterm, ROUND(AVG(g.final_score), 2) AS avg_final FROM grades g JOIN enrollments e ON g.grade_id = e.enrollment_id JOIN courses c ON e.course_id = c.course_id JOIN departments d ON c.department_id = d.department_id GROUP BY d.department_id ORDER BY avg_final DESC;', 'SELECT d.name AS department_name, ROUND(AVG(g.midterm_score), 2) AS avg_midterm, ROUND(AVG(g.final_score), 2) AS avg_final FROM grades g JOIN enrollments e ON g.grade_id = e.enrollment_id JOIN courses c ON e.course_id = c.course_id JOIN departments d ON c.department_id = d.department_id GROUP BY d.department_id ORDER BY avg_final DESC;'), q(153, 'medium', ['Join', 'company:Salesforce', 'topic:ER Modeling'], 'Show courses along with their prerequisite course title. Show course title and prereq_title (use LEFT JOIN for courses with no prereq).', 'Self-JOIN on courses: c1 LEFT JOIN courses c2 ON c1.prereq_course_id = c2.course_id.', 'LEFT JOIN courses ON prereq_course_id = course_id', 'SELECT c1.title, c2.title AS prereq_title FROM courses c1 LEFT JOIN courses c2 ON c1.prereq_course_id = c2.course_id ORDER BY c1.title;', 'SELECT c1.title, c2.title AS prereq_title FROM courses c1 LEFT JOIN courses c2 ON c1.prereq_course_id = c2.course_id ORDER BY c1.title;'), q(154, 'medium', ['Group By', 'Having', 'company:Accenture', 'topic:Execution Order'], 'Find students who received at least one F grade. Show student_id and count_of_Fs.', 'JOIN enrollments → grades, filter letter_grade = F.', 'WHERE letter_grade = \'F\'', 'SELECT e.student_id, COUNT(*) AS count_of_Fs FROM enrollments e JOIN grades g ON e.enrollment_id = g.grade_id WHERE g.letter_grade = \'F\' GROUP BY e.student_id;', 'SELECT e.student_id, COUNT(*) AS count_of_Fs FROM enrollments e JOIN grades g ON e.enrollment_id = g.grade_id WHERE g.letter_grade = \'F\' GROUP BY e.student_id;'), q(155, 'medium', ['Join', 'Group By', 'company:Google', 'topic:Execution Order'], 'Show how many courses each student has completed (total enrolled). Show student name and total_courses.', 'JOIN students → enrollments, COUNT, GROUP BY.', 'COUNT(enrollment_id) GROUP BY student_id', "SELECT s.first_name || ' ' || s.last_name AS student_name, COUNT(e.enrollment_id) AS total_courses FROM students s LEFT JOIN enrollments e ON s.student_id = e.student_id GROUP BY s.student_id ORDER BY total_courses DESC;", "SELECT s.first_name || ' ' || s.last_name AS student_name, COUNT(e.enrollment_id) AS total_courses FROM students s LEFT JOIN enrollments e ON s.student_id = e.student_id GROUP BY s.student_id ORDER BY total_courses DESC;"), q(156, 'medium', ['Join', 'Group By', 'company:Meta', 'topic:Normalization'], 'Find the most popular major (by student count). Show major and count.', 'GROUP BY major, ORDER DESC, LIMIT 1.', 'ORDER BY COUNT(*) DESC LIMIT 1', 'SELECT major, COUNT(*) AS student_count FROM students GROUP BY major ORDER BY student_count DESC LIMIT 1;', 'SELECT major, COUNT(*) AS student_count FROM students GROUP BY major ORDER BY student_count DESC LIMIT 1;'), q(157, 'medium', ['Join', 'Date Function', 'company:Airbnb', 'topic:Execution Order'], 'Calculate how long each professor has been employed (in years). Show professor name and years_employed.', 'julianday(current) - julianday(hired_at) / 365.', 'ROUND(julianday(\'now\') - julianday(hired_at)) / 365', "SELECT first_name || ' ' || last_name AS professor_name, ROUND((julianday('2024-01-01') - julianday(hired_at)) / 365, 1) AS years_employed FROM professors ORDER BY years_employed DESC;", "SELECT first_name || ' ' || last_name AS professor_name, ROUND((julianday('2024-01-01') - julianday(hired_at)) / 365, 1) AS years_employed FROM professors ORDER BY years_employed DESC;"), q(158, 'medium', ['Join', 'Group By', 'company:Goldman Sachs', 'topic:Normalization'], 'Show the pass rate per course (percentage of A or B grades). Show title and pass_rate_pct.', 'SUM(CASE WHEN letter_grade IN (A,B) THEN 1 ELSE 0 END) / COUNT(*) * 100.', 'SUM(CASE WHEN letter_grade IN (\'A\',\'B\') THEN 1 ELSE 0 END) * 100.0 / COUNT(*)', 'SELECT c.title, ROUND(100.0 * SUM(CASE WHEN g.letter_grade IN (\'A\',\'B\') THEN 1 ELSE 0 END) / COUNT(*), 1) AS pass_rate_pct FROM courses c JOIN enrollments e ON c.course_id = e.course_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY c.course_id ORDER BY pass_rate_pct DESC;', 'SELECT c.title, ROUND(100.0 * SUM(CASE WHEN g.letter_grade IN (\'A\',\'B\') THEN 1 ELSE 0 END) / COUNT(*), 1) AS pass_rate_pct FROM courses c JOIN enrollments e ON c.course_id = e.course_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY c.course_id ORDER BY pass_rate_pct DESC;'), q(159, 'medium', ['Join', 'Group By', 'company:Oracle', 'topic:Subqueries'], 'List professors grouped by department with a count. Show department_name and professor_count.', 'JOIN professors → departments, COUNT, GROUP BY.', 'COUNT(professor_id) GROUP BY department_id', 'SELECT d.name AS department_name, COUNT(p.professor_id) AS professor_count FROM departments d LEFT JOIN professors p ON d.department_id = p.department_id GROUP BY d.department_id, d.name ORDER BY professor_count DESC;', 'SELECT d.name AS department_name, COUNT(p.professor_id) AS professor_count FROM departments d LEFT JOIN professors p ON d.department_id = p.department_id GROUP BY d.department_id, d.name ORDER BY professor_count DESC;'), q(160, 'medium', ['Join', 'Group By', 'company:Uber', 'topic:Execution Order'], 'Show total credit hours enrolled per student (sum of all enrolled course credit_hours). Show student name and total_credits.', 'JOIN students → enrollments → courses, SUM(credit_hours).', 'SUM(credit_hours) GROUP BY student_id', "SELECT s.first_name || ' ' || s.last_name AS student_name, SUM(c.credit_hours) AS total_credits FROM students s JOIN enrollments e ON s.student_id = e.student_id JOIN courses c ON e.course_id = c.course_id GROUP BY s.student_id ORDER BY total_credits DESC;", "SELECT s.first_name || ' ' || s.last_name AS student_name, SUM(c.credit_hours) AS total_credits FROM students s JOIN enrollments e ON s.student_id = e.student_id JOIN courses c ON e.course_id = c.course_id GROUP BY s.student_id ORDER BY total_credits DESC;"), q(161, 'medium', ['Join', 'Group By', 'company:Uber', 'topic:Execution Order'], "Identify the student who is enrolled in the most courses (across all semesters). Show student name and course_count.", 'GROUP BY student, COUNT, ORDER DESC LIMIT 1.', 'COUNT(enrollment_id) ORDER BY COUNT DESC LIMIT 1', "SELECT s.first_name || ' ' || s.last_name AS student_name, COUNT(e.enrollment_id) AS course_count FROM students s JOIN enrollments e ON s.student_id = e.student_id GROUP BY s.student_id ORDER BY course_count DESC LIMIT 1;", "SELECT s.first_name || ' ' || s.last_name AS student_name, COUNT(e.enrollment_id) AS course_count FROM students s JOIN enrollments e ON s.student_id = e.student_id GROUP BY s.student_id ORDER BY course_count DESC LIMIT 1;"), q(162, 'medium', ['Join', 'Group By', 'company:Uber', 'topic:Execution Order'], 'Calculate the average final_score per semester. Show semester name and avg_score.', 'JOIN grades → enrollments → semesters, AVG.', 'AVG(final_score) GROUP BY semester_id', 'SELECT s.name AS semester_name, ROUND(AVG(g.final_score), 2) AS avg_score FROM semesters s JOIN enrollments e ON s.semester_id = e.semester_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY s.semester_id ORDER BY avg_score DESC;', 'SELECT s.name AS semester_name, ROUND(AVG(g.final_score), 2) AS avg_score FROM semesters s JOIN enrollments e ON s.semester_id = e.semester_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY s.semester_id ORDER BY avg_score DESC;'), q(163, 'medium', ['Join', 'Group By', 'company:Microsoft', 'topic:Normalization'], "Find the department with the highest average student GPA (avg final_score). Show department_name and avg_gpa.", 'Chain JOIN to get from grades → enrollments → courses → departments. AVG final_score.', 'AVG(final_score) GROUP BY department ORDER BY AVG DESC LIMIT 1', 'SELECT d.name AS department_name, ROUND(AVG(g.final_score), 2) AS avg_gpa FROM grades g JOIN enrollments e ON g.grade_id = e.enrollment_id JOIN courses c ON e.course_id = c.course_id JOIN departments d ON c.department_id = d.department_id GROUP BY d.department_id ORDER BY avg_gpa DESC LIMIT 1;', 'SELECT d.name AS department_name, ROUND(AVG(g.final_score), 2) AS avg_gpa FROM grades g JOIN enrollments e ON g.grade_id = e.enrollment_id JOIN courses c ON e.course_id = c.course_id JOIN departments d ON c.department_id = d.department_id GROUP BY d.department_id ORDER BY avg_gpa DESC LIMIT 1;'), q(164, 'medium', ['Join', 'Group By', 'company:Goldman Sachs', 'topic:ER Modeling'], 'Show how many students are enrolled in each section (A/B/C). Show section and student_count.', 'GROUP BY section on enrollments.', 'SELECT section, COUNT(*) FROM enrollments GROUP BY section', 'SELECT section, COUNT(*) AS student_count FROM enrollments GROUP BY section ORDER BY student_count DESC;', 'SELECT section, COUNT(*) AS student_count FROM enrollments GROUP BY section ORDER BY student_count DESC;'), q(165, 'medium', ['Join', 'Group By', 'company:Stripe', 'topic:Views'], 'List all students enrolled in the current semester (semester_id = 5). Show first_name and last_name.', 'JOIN enrollments to students, filter semester_id = 5.', 'WHERE semester_id = 5', 'SELECT DISTINCT s.first_name, s.last_name FROM students s JOIN enrollments e ON s.student_id = e.student_id WHERE e.semester_id = 5;', 'SELECT DISTINCT s.first_name, s.last_name FROM students s JOIN enrollments e ON s.student_id = e.student_id WHERE e.semester_id = 5;'),
// HARD
q(166, 'hard', ['Window Function', 'Rank', 'company:Meta', 'topic:Indexing'], 'Rank students by their average final score using DENSE_RANK. Show student name, avg_score, and rank.', 'DENSE_RANK() OVER (ORDER BY avg_score DESC).', 'DENSE_RANK() OVER (ORDER BY avg_score DESC) AS student_rank', "SELECT student_name, avg_score, DENSE_RANK() OVER (ORDER BY avg_score DESC) AS student_rank FROM (SELECT s.first_name || ' ' || s.last_name AS student_name, ROUND(AVG(g.final_score), 2) AS avg_score FROM students s JOIN enrollments e ON s.student_id = e.student_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY s.student_id) ORDER BY student_rank;", "SELECT student_name, avg_score, DENSE_RANK() OVER (ORDER BY avg_score DESC) AS student_rank FROM (SELECT s.first_name || ' ' || s.last_name AS student_name, ROUND(AVG(g.final_score), 2) AS avg_score FROM students s JOIN enrollments e ON s.student_id = e.student_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY s.student_id) ORDER BY student_rank;"), q(167, 'hard', ['Window Function', 'Row Number', 'company:Salesforce', 'topic:Locks'], 'For each department, find the top-scoring student using ROW_NUMBER(). Show department_name, student_name, avg_score, and row_num where row_num = 1.', 'ROW_NUMBER() OVER (PARTITION BY department ORDER BY avg_score DESC).', 'ROW_NUMBER() OVER (PARTITION BY department_id ORDER BY avg_score DESC) AS rn WHERE rn = 1', "SELECT department_name, student_name, avg_score FROM (SELECT d.name AS department_name, s.first_name || ' ' || s.last_name AS student_name, ROUND(AVG(g.final_score), 2) AS avg_score, ROW_NUMBER() OVER (PARTITION BY d.department_id ORDER BY AVG(g.final_score) DESC) AS rn FROM students s JOIN enrollments e ON s.student_id = e.student_id JOIN grades g ON e.enrollment_id = g.grade_id JOIN courses c ON e.course_id = c.course_id JOIN departments d ON c.department_id = d.department_id GROUP BY d.department_id, s.student_id) WHERE rn = 1 ORDER BY department_name;", "SELECT department_name, student_name, avg_score FROM (SELECT d.name AS department_name, s.first_name || ' ' || s.last_name AS student_name, ROUND(AVG(g.final_score), 2) AS avg_score, ROW_NUMBER() OVER (PARTITION BY d.department_id ORDER BY AVG(g.final_score) DESC) AS rn FROM students s JOIN enrollments e ON s.student_id = e.student_id JOIN grades g ON e.enrollment_id = g.grade_id JOIN courses c ON e.course_id = c.course_id JOIN departments d ON c.department_id = d.department_id GROUP BY d.department_id, s.student_id) WHERE rn = 1 ORDER BY department_name;"), q(168, 'hard', ['CTE', 'Recursive CTE', 'company:Google', 'topic:Query Optimization'], 'Use a recursive CTE to build the course prerequisite chain. Show course title and its direct prerequisite title at every level.', 'Self-JOIN on prereq_course_id or recursive CTE starting from courses with no prereqs.', 'WITH RECURSIVE prereq_chain AS (SELECT ... UNION ALL SELECT ...)', "WITH RECURSIVE prereq_chain AS (SELECT course_id, title, prereq_course_id, title AS root_title, 0 AS depth FROM courses WHERE prereq_course_id IS NULL UNION ALL SELECT c.course_id, c.title, c.prereq_course_id, pc.root_title, pc.depth + 1 FROM courses c JOIN prereq_chain pc ON c.prereq_course_id = pc.course_id) SELECT title AS course_title, root_title AS root_course, depth FROM prereq_chain ORDER BY root_title, depth;", "WITH RECURSIVE prereq_chain AS (SELECT course_id, title, prereq_course_id, title AS root_title, 0 AS depth FROM courses WHERE prereq_course_id IS NULL UNION ALL SELECT c.course_id, c.title, c.prereq_course_id, pc.root_title, pc.depth + 1 FROM courses c JOIN prereq_chain pc ON c.prereq_course_id = pc.course_id) SELECT title AS course_title, root_title AS root_course, depth FROM prereq_chain ORDER BY root_title, depth;"), q(169, 'hard', ['Window Function', 'Lag', 'company:Airbnb', 'topic:Indexing'], 'For each student with multiple enrollments, compare their grade score in each semester to the previous one using LAG. Show student_id, semester_id, avg_score, and prev_score.', 'LAG(avg_score) OVER (PARTITION BY student_id ORDER BY semester_id).', 'LAG(avg_score) OVER (PARTITION BY student_id ORDER BY semester_id) AS prev_score', 'SELECT student_id, semester_id, avg_score, LAG(avg_score) OVER (PARTITION BY student_id ORDER BY semester_id) AS prev_score FROM (SELECT e.student_id, e.semester_id, ROUND(AVG(g.final_score), 2) AS avg_score FROM enrollments e JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY e.student_id, e.semester_id) ORDER BY student_id, semester_id;', 'SELECT student_id, semester_id, avg_score, LAG(avg_score) OVER (PARTITION BY student_id ORDER BY semester_id) AS prev_score FROM (SELECT e.student_id, e.semester_id, ROUND(AVG(g.final_score), 2) AS avg_score FROM enrollments e JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY e.student_id, e.semester_id) ORDER BY student_id, semester_id;'), q(170, 'hard', ['CTE', 'Window Function', 'company:Google', 'topic:CTEs'], 'Find students whose final score improved (was higher) in every subsequent enrollment compared to their first enrollment. Show student_id and first_score.', 'Use LAG to compare scores per student ordered by enrollment_id.', 'LAG(final_score) OVER (PARTITION BY student_id ORDER BY enrollment_id)', 'WITH scored AS (SELECT e.student_id, e.enrollment_id, g.final_score, LAG(g.final_score) OVER (PARTITION BY e.student_id ORDER BY e.enrollment_id) AS prev_score FROM enrollments e JOIN grades g ON e.enrollment_id = g.grade_id), improving AS (SELECT student_id, MIN(final_score) AS first_score, SUM(CASE WHEN prev_score IS NOT NULL AND final_score <= prev_score THEN 1 ELSE 0 END) AS bad_cnt FROM scored GROUP BY student_id HAVING bad_cnt = 0 AND COUNT(*) > 1) SELECT student_id, first_score FROM improving ORDER BY student_id;', 'WITH scored AS (SELECT e.student_id, e.enrollment_id, g.final_score, LAG(g.final_score) OVER (PARTITION BY e.student_id ORDER BY e.enrollment_id) AS prev_score FROM enrollments e JOIN grades g ON e.enrollment_id = g.grade_id), improving AS (SELECT student_id, MIN(final_score) AS first_score, SUM(CASE WHEN prev_score IS NOT NULL AND final_score <= prev_score THEN 1 ELSE 0 END) AS bad_cnt FROM scored GROUP BY student_id HAVING bad_cnt = 0 AND COUNT(*) > 1) SELECT student_id, first_score FROM improving ORDER BY student_id;'), q(171, 'hard', ['Window Function', 'Rank', 'CTE', 'company:JPMorgan Chase', 'topic:Query Optimization'], 'Show the top 2 courses by enrollment in each semester using RANK(). Show semester_name, course title, enrollment_count, and rank for rank <= 2.', 'RANK() OVER (PARTITION BY semester ORDER BY COUNT DESC), filter rank <= 2.', 'RANK() OVER (PARTITION BY semester_id ORDER BY enrollment_count DESC) AS rn WHERE rn <= 2', 'WITH ranked AS (SELECT s.name AS semester_name, c.title, COUNT(e.enrollment_id) AS enrollment_count, RANK() OVER (PARTITION BY s.semester_id ORDER BY COUNT(e.enrollment_id) DESC) AS rn FROM semesters s JOIN enrollments e ON s.semester_id = e.semester_id JOIN courses c ON e.course_id = c.course_id GROUP BY s.semester_id, c.course_id) SELECT * FROM ranked WHERE rn <= 2 ORDER BY semester_name, rn;', 'WITH ranked AS (SELECT s.name AS semester_name, c.title, COUNT(e.enrollment_id) AS enrollment_count, RANK() OVER (PARTITION BY s.semester_id ORDER BY COUNT(e.enrollment_id) DESC) AS rn FROM semesters s JOIN enrollments e ON s.semester_id = e.semester_id JOIN courses c ON e.course_id = c.course_id GROUP BY s.semester_id, c.course_id) SELECT * FROM ranked WHERE rn <= 2 ORDER BY semester_name, rn;'), q(172, 'hard', ['Window Function', 'CTE', 'company:Accenture', 'topic:Indexing'], 'Calculate a rolling average of final_score for each student over their enrollments (ordered by enrollment_id). Show student_id, enrollment_id, final_score, rolling_avg.', 'AVG(final_score) OVER (PARTITION BY student_id ORDER BY enrollment_id ROWS UNBOUNDED PRECEDING).', 'AVG(final_score) OVER (PARTITION BY student_id ORDER BY enrollment_id ROWS UNBOUNDED PRECEDING) AS rolling_avg', 'SELECT e.student_id, e.enrollment_id, g.final_score, ROUND(AVG(g.final_score) OVER (PARTITION BY e.student_id ORDER BY e.enrollment_id ROWS UNBOUNDED PRECEDING), 2) AS rolling_avg FROM enrollments e JOIN grades g ON e.enrollment_id = g.grade_id ORDER BY e.student_id, e.enrollment_id;', 'SELECT e.student_id, e.enrollment_id, g.final_score, ROUND(AVG(g.final_score) OVER (PARTITION BY e.student_id ORDER BY e.enrollment_id ROWS UNBOUNDED PRECEDING), 2) AS rolling_avg FROM enrollments e JOIN grades g ON e.enrollment_id = g.grade_id ORDER BY e.student_id, e.enrollment_id;'), q(173, 'hard', ['CTE', 'Join', 'company:Oracle', 'topic:CTEs'], 'Find pairs of students who share the same advisor and are enrolled in at least one common course. Show student1_name, student2_name, and shared_advisor_id.', 'Self-JOIN enrollments on course_id to find common courses. JOIN students to get advisor.', 'JOIN e1 ON course_id = e2.course_id WHERE e1.student_id < e2.student_id AND s1.advisor_id = s2.advisor_id', "SELECT DISTINCT s1.first_name || ' ' || s1.last_name AS student1_name, s2.first_name || ' ' || s2.last_name AS student2_name, s1.advisor_id AS shared_advisor_id FROM enrollments e1 JOIN enrollments e2 ON e1.course_id = e2.course_id AND e1.student_id < e2.student_id JOIN students s1 ON e1.student_id = s1.student_id JOIN students s2 ON e2.student_id = s2.student_id WHERE s1.advisor_id = s2.advisor_id AND s1.advisor_id IS NOT NULL ORDER BY shared_advisor_id;", "SELECT DISTINCT s1.first_name || ' ' || s1.last_name AS student1_name, s2.first_name || ' ' || s2.last_name AS student2_name, s1.advisor_id AS shared_advisor_id FROM enrollments e1 JOIN enrollments e2 ON e1.course_id = e2.course_id AND e1.student_id < e2.student_id JOIN students s1 ON e1.student_id = s1.student_id JOIN students s2 ON e2.student_id = s2.student_id WHERE s1.advisor_id = s2.advisor_id AND s1.advisor_id IS NOT NULL ORDER BY shared_advisor_id;"), q(174, 'hard', ['Window Function', 'Rank', 'company:NVIDIA', 'topic:CTEs'], 'Calculate NTILE(3) (thirds) of students by their average grade score. Show student_name, avg_score, and tier (1=top third, 3=bottom third).', 'NTILE(3) OVER (ORDER BY avg_score DESC).', 'NTILE(3) OVER (ORDER BY avg_score DESC) AS tier', "SELECT student_name, avg_score, NTILE(3) OVER (ORDER BY avg_score DESC) AS tier FROM (SELECT s.first_name || ' ' || s.last_name AS student_name, ROUND(AVG(g.final_score), 2) AS avg_score FROM students s JOIN enrollments e ON s.student_id = e.student_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY s.student_id) ORDER BY tier, avg_score DESC;", "SELECT student_name, avg_score, NTILE(3) OVER (ORDER BY avg_score DESC) AS tier FROM (SELECT s.first_name || ' ' || s.last_name AS student_name, ROUND(AVG(g.final_score), 2) AS avg_score FROM students s JOIN enrollments e ON s.student_id = e.student_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY s.student_id) ORDER BY tier, avg_score DESC;"), q(175, 'hard', ['CTE', 'Window Function', 'Rank', 'company:Google', 'topic:Window Functions'], 'Use a CTE to identify professors whose advisees consistently score above 85. Show professor_name and avg_advisee_score. Only include professors with at least 2 advisees.', "CTE: compute avg score per professor's advisees. Filter WHERE avg > 85 AND count >= 2.", "WITH scores AS (...) SELECT ... WHERE avg_score > 85 AND advisee_count >= 2", "WITH scores AS (SELECT p.professor_id, p.first_name || ' ' || p.last_name AS professor_name, COUNT(DISTINCT s.student_id) AS advisee_count, ROUND(AVG(g.final_score), 2) AS avg_advisee_score FROM professors p JOIN students s ON p.professor_id = s.advisor_id JOIN enrollments e ON s.student_id = e.student_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY p.professor_id) SELECT professor_name, avg_advisee_score FROM scores WHERE avg_advisee_score > 85 AND advisee_count >= 2 ORDER BY avg_advisee_score DESC;", "WITH scores AS (SELECT p.professor_id, p.first_name || ' ' || p.last_name AS professor_name, COUNT(DISTINCT s.student_id) AS advisee_count, ROUND(AVG(g.final_score), 2) AS avg_advisee_score FROM professors p JOIN students s ON p.professor_id = s.advisor_id JOIN enrollments e ON s.student_id = e.student_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY p.professor_id) SELECT professor_name, avg_advisee_score FROM scores WHERE avg_advisee_score > 85 AND advisee_count >= 2 ORDER BY avg_advisee_score DESC;"), q(176, 'hard', ['Window Function', 'Lead', 'company:Meta', 'topic:Window Functions'], "For each course enrollment, show the next course the student enrolled in (ordered by enrollment_id). Show student_id, current_course_title, and next_course_title.", 'LEAD on course title OVER PARTITION BY student, ORDER BY enrollment_id.', 'LEAD(course_title) OVER (PARTITION BY student_id ORDER BY enrollment_id) AS next_course', "SELECT e.student_id, c.title AS current_course_title, LEAD(c.title) OVER (PARTITION BY e.student_id ORDER BY e.enrollment_id) AS next_course_title FROM enrollments e JOIN courses c ON e.course_id = c.course_id ORDER BY e.student_id, e.enrollment_id;", "SELECT e.student_id, c.title AS current_course_title, LEAD(c.title) OVER (PARTITION BY e.student_id ORDER BY e.enrollment_id) AS next_course_title FROM enrollments e JOIN courses c ON e.course_id = c.course_id ORDER BY e.student_id, e.enrollment_id;"), q(177, 'hard', ['CTE', 'Join', 'company:ServiceNow', 'topic:Window Functions'], 'Find courses where all enrolled students received an A grade. Show course title and total_students.', 'GROUP BY course, HAVING MIN(letter_grade) = MAX(letter_grade) = A, or use HAVING SUM(CASE WHEN letter_grade != A) = 0.', 'HAVING SUM(CASE WHEN g.letter_grade != \'A\' THEN 1 ELSE 0 END) = 0', 'SELECT c.title, COUNT(e.enrollment_id) AS total_students FROM courses c JOIN enrollments e ON c.course_id = e.course_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY c.course_id HAVING SUM(CASE WHEN g.letter_grade != \'A\' THEN 1 ELSE 0 END) = 0 ORDER BY total_students DESC;', 'SELECT c.title, COUNT(e.enrollment_id) AS total_students FROM courses c JOIN enrollments e ON c.course_id = e.course_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY c.course_id HAVING SUM(CASE WHEN g.letter_grade != \'A\' THEN 1 ELSE 0 END) = 0 ORDER BY total_students DESC;'), q(178, 'hard', ['CTE', 'Window Function', 'company:TCS', 'topic:Transactions'], 'Create a semester-over-semester comparison: for each course, show enrollment in Fall 2023 vs Fall 2022, and the change. Show course_title, fall23_count, fall22_count, change.', 'Use conditional aggregation: SUM(CASE WHEN semester_id = 3 THEN 1 END).', 'SUM(CASE WHEN semester_id = 3 THEN 1 ELSE 0 END) AS fall23_count', 'SELECT c.title AS course_title, SUM(CASE WHEN e.semester_id = 3 THEN 1 ELSE 0 END) AS fall23_count, SUM(CASE WHEN e.semester_id = 1 THEN 1 ELSE 0 END) AS fall22_count, SUM(CASE WHEN e.semester_id = 3 THEN 1 ELSE 0 END) - SUM(CASE WHEN e.semester_id = 1 THEN 1 ELSE 0 END) AS change FROM courses c LEFT JOIN enrollments e ON c.course_id = e.course_id GROUP BY c.course_id HAVING fall23_count > 0 OR fall22_count > 0 ORDER BY change DESC;', 'SELECT c.title AS course_title, SUM(CASE WHEN e.semester_id = 3 THEN 1 ELSE 0 END) AS fall23_count, SUM(CASE WHEN e.semester_id = 1 THEN 1 ELSE 0 END) AS fall22_count, SUM(CASE WHEN e.semester_id = 3 THEN 1 ELSE 0 END) - SUM(CASE WHEN e.semester_id = 1 THEN 1 ELSE 0 END) AS change FROM courses c LEFT JOIN enrollments e ON c.course_id = e.course_id GROUP BY c.course_id HAVING fall23_count > 0 OR fall22_count > 0 ORDER BY change DESC;'), q(179, 'hard', ['Window Function', 'Rank', 'CTE', 'company:ServiceNow', 'topic:CTEs'], 'Identify students who scored in the top 10% in their major (by average final score). Show student name, major, avg_score, and percentile.', 'Use PERCENT_RANK() or NTILE(10) OVER (PARTITION BY major ORDER BY avg_score DESC). Show NTILE=1.', 'NTILE(10) OVER (PARTITION BY major ORDER BY avg_score DESC) AS decile WHERE decile = 1', "WITH scored AS (SELECT s.student_id, s.first_name || ' ' || s.last_name AS student_name, s.major, ROUND(AVG(g.final_score), 2) AS avg_score FROM students s JOIN enrollments e ON s.student_id = e.student_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY s.student_id) SELECT student_name, major, avg_score, NTILE(10) OVER (PARTITION BY major ORDER BY avg_score DESC) AS decile FROM scored WHERE decile = 1 ORDER BY major, avg_score DESC;", "WITH scored AS (SELECT s.student_id, s.first_name || ' ' || s.last_name AS student_name, s.major, ROUND(AVG(g.final_score), 2) AS avg_score FROM students s JOIN enrollments e ON s.student_id = e.student_id JOIN grades g ON e.enrollment_id = g.grade_id GROUP BY s.student_id) SELECT student_name, major, avg_score, NTILE(10) OVER (PARTITION BY major ORDER BY avg_score DESC) AS decile FROM scored WHERE decile = 1 ORDER BY major, avg_score DESC;"), q(180, 'hard', ['Window Function', 'CTE', 'company:Databricks', 'topic:Transactions'], "Calculate cumulative credit hours earned per student across semesters (ordered by semester start_date). Show student_id, semester_name, semester_credits, and running_total_credits.", 'SUM(credit_hours) OVER (PARTITION BY student ORDER BY semester start_date ROWS UNBOUNDED PRECEDING).', 'SUM(semester_credits) OVER (PARTITION BY student_id ORDER BY start_date ROWS UNBOUNDED PRECEDING)', 'WITH sem_credits AS (SELECT e.student_id, s.name AS semester_name, s.start_date, SUM(c.credit_hours) AS semester_credits FROM enrollments e JOIN courses c ON e.course_id = c.course_id JOIN semesters s ON e.semester_id = s.semester_id GROUP BY e.student_id, s.semester_id) SELECT student_id, semester_name, semester_credits, SUM(semester_credits) OVER (PARTITION BY student_id ORDER BY start_date ROWS UNBOUNDED PRECEDING) AS running_total_credits FROM sem_credits ORDER BY student_id, start_date;', 'WITH sem_credits AS (SELECT e.student_id, s.name AS semester_name, s.start_date, SUM(c.credit_hours) AS semester_credits FROM enrollments e JOIN courses c ON e.course_id = c.course_id JOIN semesters s ON e.semester_id = s.semester_id GROUP BY e.student_id, s.semester_id) SELECT student_id, semester_name, semester_credits, SUM(semester_credits) OVER (PARTITION BY student_id ORDER BY start_date ROWS UNBOUNDED PRECEDING) AS running_total_credits FROM sem_credits ORDER BY student_id, start_date;')];